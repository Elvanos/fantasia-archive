/**
 * Minimal Pinia dialog-component store shape for open-routing (dialogToOpen / dialogUUID).
 */
export interface I_dialogComponentStoreLike {
  dialogToOpen?: unknown
  dialogUUID?: unknown
  projectMediaRequestedPanel?: unknown
}
