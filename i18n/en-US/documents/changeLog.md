# Changelog
----------

## 2.4.16

### New features
- **Project overview**: With no document tabs open, **`/home`** shows a stacked **document distribution** chart by world and template, empty-state CTAs (**Create your first document Template!**, **Assign your template to your world!**, **Create your first document!**), and a **Last opened** list (middle-click opens in the background; right-click matches hierarchy document actions).
- **Quick Add**: App control bar **Quick add** (and its keybind) opens **Add New Document** — pick a world when needed, then a template from that world's layout — and creates a temporary opened document tab. The last chosen world is remembered in the project file for both Quick Add and Quick Search.
- **Quick Search**: App control bar **Quick-search**, Content menu **Quick-Search Existing Document**, and **Ctrl+Q** open **Search through existing documents** — pick a world when needed, filter documents by name (categories included), open in preview on select, or use Edit / Copy / Add under on each row (left-click closes the dialog and focuses the tab; middle-click adds the tab in the background without switching away, and keeps search open so you can keep picking). With **Prevent close after selection on quick-search popup** on, select/Enter also keeps search open and only appends the document in the background (no active-tab switch).
- **App controls**: **Toggle Hierarchical Tree** sits under **Toggle App Noteboard** and uses the same shortcut as the control-bar tree button (**Ctrl+Alt+Shift+T**). The row is disabled when no project is open.
- **Project Media**: App control bar icon after **Toggle tree** (hidden with **Disable app control bar function buttons**), Content menu **Project Media** after **Quick-Search Existing Document**, and **Ctrl+Alt+Shift+M** (**Cmd+Alt+Shift+M** on macOS) open **Project Media** — title, **Search media...**, and **Close**. Search does not filter yet. Pressing the same shortcut again closes the dialog. With no project open, the menu row is disabled and the shortcut does nothing; the bar icon stays visible.
- **Projects**: Opening or saving documents updates a per-project **last opened** MRU (schema **`user_version` 8**, table **`document_last_opened`**, newest first, capped).
- **Project tags**: Each world keeps a tag list in the hierarchy tree (**Tags** wrapper, or flat when **Compact tags** is on). Assign tags on a document via the workspace **Tags** field (**FaSelectInput** tags mode, create-new supported). Empty unused tags are removed after save. Right-click a tag to rename (merge on name clash), delete, add a new document under a placement into that tag, or sort documents under the tag. Drag reorder under a tag is reserved for a later pass.
- **App Settings**: Under **Hierarchical tree**, **Compact tags**, **No tags**, and **Tags at the top** control whether tags appear, how they nest under a world, and whether the tags block sits above or below template layout rows.
- **App Settings**: **Disable spellcheck** under **Visuals & app-wide functionality** turns Chromium spellcheck on or off immediately for the current interface language (no app restart). The former restart-required note is gone.
- **Help**: **Check for updates** (between **Tips, Tricks & Trivia** and **About Fantasia Archive**) compares the installed version to the latest GitHub release. When a newer release exists, an info notification shows the version, Fantasia reading mascot, and a **Download** button that opens the releases page. The same check also runs once at app start. If you are already on the newest version, only a manual Help check shows a success notification. **Fantasia Archive Settings** → **Visuals & app-wide functionality** includes **Disable start update check message** to turn off the automatic start check while keeping the Help menu action.
- **Controls**: Reusable **FaSelectInput** select fields support chips or an **inline** selected row (icon + label, no chips), filter highlighting of whole matching words, optional create-new, colored option icons, and document / template / simple / **tags** modes. Dropdown menus use a fixed **600px** width, center under the field when space allows, fill taller lists on first open, and use low-opacity separators between options. **Tab** into the field opens the menu; choosing the same option again still counts as an activation (so parents can reopen the next step).

### Bugfixes & Optimizations
- **Workspace sidebar**: Restoring a large expanded hierarchy tree is much faster. Document children now load from one project-wide document list instead of a separate request for each expanded parent.
- **Workspace sidebar**: Clicking the resize splitter without dragging no longer overwrites the saved sidebar width.
- **Fantasy theme**: Menu hover gold glow stays on the row and does not peek past the last item. The welcome **Resume Latest Project** menu inner rim is a bit softer.
- **Document workspace**: Tab context menu **Move tab left** / **Move tab right** use double-angle icons. **Close this tab** and the other close-tab rows use the same ink as copy/move actions, not secondary. Trailing menu icons match the Last opened glyph box.
- **Controls**: **FaSelectInput** fantasy dropdowns use the same bottom-edge gold hover glow as other menus; label and icon colors stay idle (no wash tint). Selected-row side bars stay on a separate layer so they do not break that glow. Option icons use the same **fa-color-glyph** path as the hierarchy tree — fantasy shading when that theme is on; flat theme keeps a solid icon color (workspace-row default, or the option **color** when set). Left option icons use the same app **`$iconSize`** box as **Last opened** document glyphs (not Quasar's larger avatar default).
- **Quick Search** / **Quick Add**: World picker is ready before the dialog opens (no late pop-in). Quick Search document list still loads after open. Document and template menu auto-open wait is **100ms** after nextTick (was 300ms).
- **App Settings**: With **Prevent close after selection on quick-search popup** on, choosing a Quick Search result only appends that document in the background — the search field stays empty of a selection, the filter list and highlighting stay put (no menu flash/refresh), and you can keep picking.
- **Controls**: **FaSelectInput** filter (including **Quick Search**) again matches Fantasia Archive 1.0 multi-word search: each space-separated query fragment must hit a different word in the option label (for example **el d** matches **The Eldritch Dialogs**), and matching words highlight in the list. Id fallback only matches the full typed needle as one substring (so queries like **af 11** no longer false-hit random document ids that happen to contain those hex fragments).
- **App Settings**: **Close quick popups with same key** (under **Quick-search/Quick-add dialog**) now works: with the toggle on, pressing the same shortcut that opened **Quick Search** or **Quick Add** closes that popup even while typing in its filter field.
- **App Settings**: Removed the unfinished **Don't precheck category filter** toggle under **Quick-search/Quick-add dialog**. Quick Search always includes categories in name search.
- **Project Settings**: **Delete Template** stays disabled while the template is assigned to any world layout (live draft), not only when documents still use it.
- **Quick Add**: World and template pickers use **FaSelectInput** (filter on both). Reselecting the same world reopens the template menu. Flat theme keeps world globe icons tinted with the world color (fantasy glyph shading unchanged).
- **Dialogs**: Top-anchored popups (**`dialogComponent--topPopup`**, including **Add New Document**) use the same small corner radius as floating windows.
- **Help / keybinds**: **Toggle developer tools** works again in installed (packaged) builds. A security guard had been blocking DevTools IPC after packaging so the menu item and shortcut did nothing.
- **Links**: Foreign http and https addresses no longer open inside the Fantasia Archive window. The app keeps only its own in-app routes; other web URLs open in your default browser.
- **Projects**: Opening an older **`.faproject`** upgrades the world color-palette column name automatically so saved world swatch lists keep working.
- **Workspace sidebar**: Hierarchy tree scroll no longer jumps to the top or leaves blank gaps when you expand or collapse rows, after drag-and-drop, or when opening a node near the bottom of a long list. **Collapse all under this node** also clears nested rows correctly again under virtualization.
- **Workspace sidebar**: Hierarchy search label is **Search the tree...**. The search glyph turns primary-bright whenever the field has text, whether or not the field is focused.
- **Workspace sidebar**: Individual tag icons in the hierarchy tree are slightly smaller so solid glyphs match outline neighbors; the main **Tags** wrapper icon stays full size.
- **Help**: Diamond help icons use a slightly thinner border.
- **Fantasy theme**: Bare primary-bright icons in field append and prepend slots keep the gold glyph (for example merge-warning icons in rename dialogs).
- **Dependencies**: Updated the bundled **DOMPurify** sanitizer (used by the code editor) to **3.4.13**, closing an in-place sanitizer bypass advisory.

## 2.4.15

### New features
- **App Settings**: **App theme** under **Visuals & app-wide functionality** lets you pick **Flat theme, Light**, **Flat theme, Dark**, **Fantasy theme, Light**, or **Fantasy theme, Dark** (replaces the old dark-mode toggle). Changing the choice previews live while the dialog is open; **Save** keeps it.
- **Themes**: **Flat** skin uses simpler Quasar-like chrome (light tooltips, flat dialog shells, thin field underlines, solid active vertical-tab indicator). **Fantasy** skin keeps parchment tooltips and notifications, dialog rim glow, gold tab edge glow, and diamond help icons.
- **App Settings**: The former **Document control bar** is now the **App control bar** (setting **Disable app control bar**, subcategory title, and related UI copy). Existing saved preferences under the old setting keys keep working after upgrade.
- **Document workspace**: **Document text color** and **Document background color** fields on each open document tab use the assigned world's color palette footer (including **+** to append a swatch to that world). Preview mode shows read-only pickers; edit mode lets you change and save them with the document. Saved colors apply to that document's tab label and icon, to its row in the hierarchy tree, and to the matching row in the tab context menu **Browse opened tabs** submenu. Multi-world projects also show the world-colored globe on document tabs and on those browse rows.
- **Document workspace**: Programmatic **create temporary opened document** support lets integrations open unsaved document tabs before the first save. Temporary tabs stay in the **opened_documents** snapshot with world, template, and optional parent metadata; first save creates the SQLite row. **Delete current document** and tab **Delete this document** stay hidden for temporary tabs until they are saved.
- **Windows**: The desktop installer uses a multi-step wizard where you can choose the install folder (for example **Program Files**). App settings and profile data still save under your user **AppData** roaming folder.
- **Workspace sidebar**: Drag the splitter beside the left panel on the **home** workspace route to resize it. Width saves per **`.faproject`** when you release the drag (minimum **375px**; no upper limit; sidebar cannot collapse to zero).
- **Workspace sidebar**: Every template placement in the hierarchy tree stays expandable. When expanded, the list shows your top-level documents (if any) and an **Add new** row with the template title (for example **Add new character**) pinned at the bottom. Left-click that row to open a new unsaved document tab; middle-click opens it in the background. Empty placements show only the add-new row.
- **Workspace sidebar**: Virtualized hierarchy tree lists each world's template layout (groups and placements) with lazy-loaded documents. Expand and collapse state plus scroll position persist per project. Drag documents to reorder within the same template placement. Left-click a leaf document row to open it in the workspace tab strip; middle-click opens a background tab.
- **Workspace sidebar**: Right-click a world, group, template placement, or parent document row in the hierarchy tree for **Expand all under this node** and **Collapse all under this node**. **Expand all under this node** opens the full subtree, including lazy-loaded document rows; **Collapse all under this node** closes the subtree and clears cached children on placement and document rows. Right-click a document row for **Copy name**, **Copy text color**, and **Copy background color** (same labels as the document tab context menu), using the saved hierarchy row name and colors. Parent document rows show expand/collapse actions first, then the copy trio. Copy actions queue through the **Action Monitor** and show a success notification when the clipboard copy succeeds. The menu uses the same row layout as document tab context menus.
- **Workspace sidebar**: Right-click a document row in the hierarchy tree for **Open document**, **Edit document**, **Copy document**, **Add new document under this**, and **Delete document** below the copy trio. **Open document** focuses the tab in preview mode; **Edit document** opens or focuses the tab in edit mode; **Copy document** creates a temporary unsaved copy tab prefilled from the saved document (name, colors, template, world, parent placement, **Category**, and status flags **Finished** / **Minor** / **Dead**) with a translated **Copy of -** name prefix, opened in edit mode; **Delete document** uses the same confirmation dialog as tab delete and works even when the document is not already open. Document actions queue through the **Action Monitor**.
- **Workspace sidebar**: Right-click a document row for **Sort by** with **Sort alphabetically** and **Sort by Custom order**, each with ascending/descending and direct-children vs recursive scope. Sorting rewrites sibling custom sort order under that parent (or through the subtree when recursive); **Custom order** badge values stay display-only and are not rewritten by sort.
- **Workspace sidebar**: **Search...** filters documents in the open project and reveals the first match in the hierarchy tree. When focused it stretches across the full window width; when the **Disable app control bar** app setting is off, a blank app control strip appears beside it on the main pane.
- **Document workspace**: Saving an open document from the control bar or keyboard shortcut queues through the **Action Monitor** (one save at a time when several are pending). A success notification reads **Document successfully saved.**
- **Document workspace**: **Alt+Shift+Left** and **Alt+Shift+Right** move the active document tab one position left or right in the workspace tab bar (silent no-op at the first or last tab).
- **Document workspace**: Drag open document tabs horizontally in the workspace tab bar to reorder them (same persisted order as **Move tab left** / **Move tab right**). Dragging near a horizontal overflow edge auto-scrolls the strip; the tab close control still closes without starting a drag.
- **Document workspace**: Right-click a document tab for **Browse opened tabs**, **Copy name**, **Copy text color**, **Copy background color**, **Move tab left**, **Move tab right**, and **Close this tab** (move actions apply to the tab you clicked; keyboard shortcuts still move the active tab). **Copy name**, **Copy text color**, and **Copy background color** queue through the **Action Monitor** and show a success notification when the clipboard copy succeeds.
- **Document workspace**: Right-click a document tab for **Copy document** and **Add new document under this** below the copy trio. **Copy document** opens a new unsaved copy tab from the tab's current drafts (or from the saved document when needed), including name, colors, template, world, parent placement, **Category**, and status flags; **Add new document under this** opens a new unsaved child tab under the same parent (or under a temporary tab when the source tab is not yet saved). Both actions queue through the **Action Monitor** and focus the new tab.
- **Document workspace**: Right-click a document tab or choose **Delete this document** to remove the document from the active **`.faproject`** after confirmation in the **Delete** dialog (permanent removal with no undo in the current project).
- **Document workspace**: The tab context menu **Browse opened tabs** submenu also lists **Close all tabs without changes**, **Close all tabs without changes except for this one**, **Force close all tabs**, and **Force close all tabs except for this one** for bulk tab cleanup.
- **Document workspace**: **Delete current document** in the control strip (preview mode) opens the same **Delete** confirmation for the active tab.
- **Document workspace**: **Belongs under** field on each open document tab sets the parent document in the hierarchy tree (empty clears the parent so the document sits at the template placement root). Preview mode shows a read-only value; edit mode lets you type a parent document id and save it with the document. Dragging the document in the hierarchy tree updates the tab field without marking the tab dirty.
- **Document workspace**: **Is a category**, **Is finished**, **Is a minor document**, and **Is Dead/Gone/Destroyed** toggles on each open document tab (preview read-only, edit writable). Saved flags persist on the document row. Finished tabs show a ✓ marker and dead tabs show † before the tab label (dead labels also use strikethrough styling).
- **Document workspace**: **Custom order** field on each open document tab stores an optional display-only number shown as a badge to the left of that document in the hierarchy tree (for visual guidelines such as years, or as a hint when using **Sort by Custom order**). Preview mode is read-only; edit mode saves the value with the document. Empty clears the badge.
- **Document workspace**: **Extra HTML classes** field on each open document tab stores space-separated CSS class names applied live on the document workspace page (styled via **Custom Project CSS**). Preview mode is read-only; edit mode saves with the document. **Copy document** keeps the value on the new tab.
- **Workspace sidebar**: Template placement rows in the hierarchy tree show document and category count badges with a tooltip breakdown (**Document count**, **Category count**, and **Document & categories count**).
- **App control bar**: Left strip buttons open **Keybind Settings**, **Advanced Search Guide**, and **Tips, Tricks & Trivia** (hidden when **Disable app control bar guides** is on). **Toggle tree** hides the workspace hierarchy sidebar and search field so the document area uses the full width; your last sidebar width restores when you show the tree again. **Toggle app noteboard** and **Toggle project noteboard** open or close those noteboard windows. **Quick add** opens **Add New Document**; **Quick-search** opens **Search through existing documents**.
- **Dialogs**: Pressing the same keyboard shortcut that opened **App Settings**, **Project Settings**, **Keybind Settings**, **Action Monitor**, or **Advanced Search Guide** again runs the same **Escape** attempt Quasar would. **App Settings**, **Project Settings**, and **Keybind Settings** stay sticky (shake / refuse) only while they have unsaved edits; when clean, Escape, outside click, and that same open shortcut dismiss them. **Action Monitor** and **Advanced Search Guide** always dismiss. Nested menus or keybind capture still take **Escape** first when they are on Quasar’s escape stack.
- **App controls**: **Toggle App Noteboard** and **Toggle Project Noteboard** show a small primary-colored corner dot on the app control bar and in the **App controls** / **Project** menus when that noteboard has saved text (empty noteboards show no dot; the project dot appears only while a project is open).
- **Fantasia Archive Settings** and **Keybind Settings**: Search and filter fields use a dedicated clear control with accessible **Clear settings search** / **Clear keybind filter** labels instead of Quasar’s default clearable field. Category tabs in **Fantasia Archive Settings** stay disabled while search is active.
- **App Settings**: **Hide hierarchical tree** under **Hierarchical tree** saves whether the workspace sidebar starts hidden (same as **Toggle tree** on the app control bar).
- **Keybind Settings**: **Toggle Hierarchical Tree** (**Ctrl+Alt+Shift+T** by default) matches the app control bar **Toggle tree** button.
- **App control bar**: Right strip adds **Copy current document** and **Add new document under this** beside edit/save (same icons as the tab context menu), for the active workspace document.
- **App Settings**: **Disable app control bar function buttons** and **Disable app control bar content buttons** hide the left-strip tree/noteboard group and the quick search/add group independently (under **Visuals & app-wide functionality** → **App control bar**).
- **App Settings**: **Prevent document previews on tabs** now lives under **Visuals & app-wide functionality** → **Tab behavior**. The separate **Open document tabs** category is removed.
- **App Settings**: **Hide tab close button** under **Visuals & app-wide functionality** → **Tab behavior** hides the **X** on open document tabs. Tabs still close via middle-click or the tab context menu.
- **App Settings**: **Show tab bar scroll buttons** under **Visuals & app-wide functionality** → **Tab behavior** shows Quasar overflow chevrons and side padding on the open-document tab strip (default off; scroll with the mouse wheel when hidden). When off, the header tab region keeps a small trailing margin and a short fade into the top bar chrome while tabs are clipped on the right (fade hides when scrolled fully to the end).
- **App Settings**: **Hide document count** removes those placement count badges from the hierarchy tree.
- **App Settings**: **Hide category count**, **Invert category position**, and **Pronounced count divider** under **Hierarchical tree** control how placement badges show category vs document counts (including a double-pipe divider when **Pronounced count divider** is on). **Project Settings** world template layout placement counts follow the same preferences.
- **App Settings**: **Hide strike-through** under **Accessibility** removes strikethrough styling on **Is Dead/Gone/Destroyed** document labels in the hierarchy tree and tab strip. Toggling the setting in the open dialog previews immediately; **Save** keeps it.
- **App Settings**: **Hide tab close button** and **Show tab bar scroll buttons** preview live while the dialog is open (same pattern as **App theme**); **Save** keeps them.
- **App Settings**: **Force sublevel collapse in the tree** under **Hierarchical tree** closes nested expand state when you close a parent (default off keeps remembering nested opens). Replaces the old **Prevent sublevel collapse** preference key; older saves still migrate.
- **Workspace sidebar**: Multi-world projects show the project name as an **h5** title above the hierarchy tree (not a tree node). **Hide project name in tree** in **App Settings** removes it.
- **App Settings**: Under **Popups & floating windows**, **Quick-search dialog** and **Universal dialog settings** merge into one subcategory **Quick-search/Quick-add dialog**.
- **App Settings**: **Prevent filled app noteboard from showing** and **Prevent filled project noteboard from showing** (defaults off). When off, a filled **App Noteboard** opens at app load and a filled **Project Noteboard** opens when a project loads; when on, that auto-open is skipped. Empty noteboards never open on their own. Manual toggle and keybinds still work. Older **Prevent filled noteboard** saves migrate to the app setting.

### Bugfixes & Optimizations
- **Checkboxes**: Fantasy checkboxes keep a transparent outline box with a gold gradient ring and checkmark (no Material hover halo). Hover tints the ring; disabled uses a muted grey ring. Flat theme keeps a simpler outlined box with primary-bright hover border.
- **Color glyphs**: Fantasy theme applies angled metal/gold shading to more chrome icons — top menus, window controls, app control bar strip and tab icons, hierarchy tree icons, **Project Settings** tab/template/palette/menu icons, color-picker **+**, help diamond ring (question mark stays solid), and document-workspace field title icons (**Belongs under**, **Custom order**, **Extra HTML classes**).
- **Document colors**: Very dark custom document text colors use a milder fantasy glyph highlight so the hue stays readable on tabs and tree icons.
- **Document tabs**: Inactive tabs marked **Is a minor document** with no custom text color use muted grey label/icon chrome; the active tab still uses the normal bright accent unless a custom text color is set.
- **Forms**: Flat-theme focused fields use a primary-bright underline or outline ring. Dark field prepend/append icons pick up fantasy glyph shading and keep warning/destructive colors when those Quasar text colors apply.
- **Scrollbars**: Shared thin pill thumbs with a darker thumb and transparent track (flat and fantasy).
- **Import / Export App Configuration**: Checkbox rows put the label on the left and the checkbox on the right with fixed column widths.
- **Project Settings**: Document-template name/icon row keeps a reserved error-message band so the icon picker stays aligned when the name field shows an error; fantasy glyph shading on related icons and palette context-menu rows.
- **Toggles**: Settings and form toggles use a fixed-size moon-pill track (**135×28**) with a sliding disc (no thumb border). Fantasy theme shades off discs in moon silver and on discs in gold with the same angled fill lighting as hierarchy world glyphs, plus a soft gold glow when on; flat theme keeps rim-only chrome without off-state glow.
- **App Settings**: Toggle controls use the same **12px** / **10px** padding as select fields so they line up under their titles; category tabs keep a **330px** minimum width.
- **Language selector**: Fantasy-theme hover glow on the spellcheck refresh control matches the red spinner icon (same edge-bloom treatment as other flat fantasy buttons).
- **Locale translations**: Fantasy-theme locale menus without column headers (single-string mode) get **10px** top padding on the scroll rows so the first field is not flush with the panel edge; singular/plural headers and row bottoms keep the matching **10px** spacing.
- **App Settings**: Category titles stay pinned above the scrollable settings list in tab mode so they no longer scroll away with the rows.
- **Project Settings**: Vertical world and document-template tab hover follows the pointer after drag-reorder instead of sticking on the wrong row.
- **Workspace sidebar**: Hierarchy tree **Custom order** badge tooltip wording matches the field name (**Custom order of the document**).
- **Workspace sidebar**: Hierarchy tree row hover chrome hugs icon and label padding, stays visible while pressed or when the row matches the active document tab, and world glyph shading applies only in the fantasy theme.
- **Social contact buttons**: Native browser title tooltips are removed; labels and icon alt text remain.
- **Forms**: Readonly dark text fields keep the framed L-corner chrome but fill with a subtle diagonal hatch and muted text so they read as non-editable (no lock icon).
- **Theme**: Dark Quasar fields use a thin grey box frame with bottom L-corner marks; fantasy-theme tooltips and notifications use parchment framing; outline/flat buttons and vertical tabs pick up soft edge-glow accents in fantasy. Quasar material ripples stay enabled app-wide; the fantasy skin hides them in CSS. Fantasy scrollbars, menu row hover glow, markdown guide headings, tables, and confirmation dialog chrome align with the fantasy palette tokens.
- **Welcome screen**: **Resume Latest Project** / **Resume Current Project** stays disabled and keeps its label while the recent project load finishes, so the primary segment no longer flickers mid-open. Splash header chrome matches the menu-button bar color.
- **App Noteboard** and **Project Noteboard**: **Close** no longer shows the toggle shortcut under the button label.
- **Delete** and **Discard changes** confirmation dialogs share one fantasy-styled content layout (left-aligned body, consistent card chrome).
- **Workspace sidebar**: Hierarchy tree rows with custom document text or background colors no longer paint a duplicate solid background behind the themed row chrome.
- **Project overview**: The tip card **Did you know** heading renders in capital letters.
- **Project Settings**: World template layout placements report separate **document** and **category** counts (aligned with hierarchy tree placement badges).
- **Help affordances**: Shared diamond **help** tooltip icon wraps help copy on settings rows and related controls.
- **Dialogs**: Tall Fantasia dialog shells (**Fantasia Archive Settings**, **Project Settings**, and others using the shared dialog card wrapper) now share one hard viewport max-height ceiling so content can no longer push the card past the window.
- **Workspace sidebar**: Opening a project or refreshing the worlds layout no longer collapses expanded hierarchy rows when the live open set is briefly empty; expand restore falls back to the saved expand ids, waits until session hydrate finishes, and skips wiping persisted expands when the tree is not ready yet.
- **App Settings**: **Hide Fantasia mascot** (title no longer includes a **TODO -** prefix) hides Fantasia everywhere that component renders, including the project overview tip card and the startup **Did you know** notification. Toggling the setting in the open settings dialog previews immediately; when the mascot is hidden, tip surfaces no longer show a help-icon placeholder.
- **App controls**: **Toggle app noteboard** in the Tools menu and on the app control bar left strip uses the clipboard-edit outline icon.
- **Document workspace**: **Copy document** (tab menu, hierarchy menu, and app control bar) now also copies **Is a category**, **Is finished**, **Is a minor document**, and **Is Dead/Gone/Destroyed** onto the new temporary tab (previously those flags always reset to off).
- **Document workspace**: Saving a **Belongs under** change reloads the new parent row in the hierarchy tree so the document stays visible without collapsing and reopening the old branch.
- **Workspace sidebar**: Expanding a hierarchy tree row after lazy load shows its document children instead of an empty open branch (tree stats stay in sync with loaded rows without remounting the whole list).
- **Workspace sidebar**: The hierarchy expand chevron rotates open smoothly when you expand a row, without blinking the rest of the tree.
- **Document workspace**: **Copy document** from a document tab no longer saves the copy at the template root when the source document has a parent in the hierarchy tree; the copy keeps the same sibling level as the source after first save.
- **Workspace sidebar**: Collapsing and reopening a template placement (for example **Buildings**) reloads deeply nested document rows again instead of leaving the lowest levels empty.
- **Workspace sidebar**: Collapsing a group no longer forgets expanded placement and document rows underneath; reopening the group or that placement restores the prior expand state.
- **Workspace sidebar**: Saving **Project Settings** world layout no longer collapses expanded worlds in the hierarchy tree.
- **Workspace sidebar**: Middle-click a template placement row (for example **Buildings**) opens a new unsaved document tab in the background, matching the **Add new** row.
- **Document workspace**: The discard-changes confirmation dialog caps at **600px** width and highlights the document name in **primary-bright** so long titles wrap instead of stretching the dialog.
- **Workspace sidebar**: Saving a document with **Document text color** or **Document background color** no longer hides that document's already-expanded child rows until you collapse and reopen the node.
- **Workspace sidebar**: After you delete an open document from the workspace, the hierarchy tree reloads the affected template placement or parent document row so the removed document disappears without reopening the project.
- **Project Settings**: Saving worlds and document templates no longer fails when the hierarchy tree already has documents in template placements (layout rows update in place instead of delete-and-reinsert).
- **Project Settings**: Saving a world layout after you add a document template no longer auto-creates placeholder **Test Document** rows in that placement.
- **Project Settings**: Saving a world layout that removes template placements (for example moving all templates back to **Available document templates**) no longer fails when those placements still had hierarchy documents; those documents are deleted with the removed placements.
- **Workspace sidebar**: The hierarchy tree refreshes after **Project Settings** save and reloads document rows under template placements.
- **Workspace sidebar**: Expanding a template placement in the hierarchy tree now lazy-loads and displays document rows from the project database.
- **Workspace sidebar**: Dragging a document in the hierarchy tree no longer changes which worlds, groups, or template placements stay expanded; collapse now clears hidden descendant expand ids, drag restore snapshots live open state (not stale persist rows), and reopen no longer walks orphan placement ids left under a collapsed parent.
- **Workspace sidebar**: The hierarchy tree shows expand and collapse controls on rows with children; template placements that contain documents open by default on first visit.
- **Workspace sidebar**: The hierarchy **Search...** row and app control strip share **48px** height; the dense search field keeps its label and icons aligned within that row.
- **Workspace sidebar**: With **Disable app control bar** on, the hierarchy **Search...** row tracks the sidebar panel width in real time while you drag the splitter (via a live width observer); saving **sidebar_width** to the project database still happens only after you release the drag.
- **App Settings**: Toggles in **Fantasia Archive Settings** update the dialog draft only until you choose **Save**; they no longer change the running app (for example the app control bar) while the dialog stays open.
- **Project overview**: Toggling **Hide tips on project overview** in **Fantasia Archive Settings** hides or shows the **Did you know** card immediately while the dialog stays open; closing without **Save** restores the previous visibility.
- **App Settings**: The **Disable app control bar** toggle title no longer includes a **TODO -** prefix.
- **Workspace sidebar**: The hierarchy **Search...** row uses the **dark-middle** (**#183e4d**) background, matching the header bar and window controls.
- **Workspace**: The app control strip and top menu strip (**Project**, **Content**, **App controls**, **Help**) keep viewport-based width when you resize the workspace sidebar splitter; they no longer shrink with the main pane or sidebar panel.
- **Workspace sidebar**: Resizing the workspace splitter now saves **sidebar_width** to the active **`.faproject`** again after you stop dragging.
- **Context menus**: Dropdown action lists use low-opacity dividers between consecutive rows in the same group (matching **Project**, **Content**, **App controls**, and **Help** menus), including the document tab context menu, hierarchy tree node menu, language selector, splash **Resume latest project** list, and **Browse opened tabs** submenu.
- **Document workspace**: Tab context menu shows a **primary-bright** divider before **Delete this document**, separating it from the force-close actions above.
- **Theme**: The **`dark-lighter`** surface color (header bar, menu dropdowns, noteboard panels, and similar chrome) is now **#194456** instead of **#234655**.
- **Top bar**: The menu strip (**Project**, **Content**, **App controls**, **Help**) has a **375px** minimum width on a **dark** background and can grow wider when translated labels need more room; the four menu buttons share the row width evenly. The rest of the header bar and window controls use **dark-middle** (**#183e4d**).
- **Top bar**: The spellcheck refresh control beside the language selector uses the **dark-middle** (**#183e4d**) background, matching the header bar and window controls.
- **Top bar menus** (**English**): The four primary **`AppControlMenus`** **`title`** strings read **Project**, **Content**, **App controls**, and **Help** (replacing **Project Management**, **Documents & Content**, **Settings & Tools**, and **Help & Info**). Other supported interface languages use the same four short menu roles in their locale files.
- **Dependencies**: Updated the bundled **DOMPurify** sanitizer (used by the code editor) to **3.4.11**, closing several sanitizer bypass advisories.
- **Project database**: The **`.faproject`** schema is flattened to a single **`user_version` 1** for this development cycle. Older pre-release **`.faproject`** files from previous dev builds are not upgraded automatically; recreate those projects after updating.
- **Project Settings**: Screen reader users get labeled controls throughout the dialog. The **Worlds** and **Document Templates** filter fields, their clear controls, the **Available document templates** filter, **World color** and **World color palette** help icons, each palette swatch, and the **World appendix** help icon now expose accessible names in every supported interface language.
- **Project Settings**: The shared delete confirmation button used by **Worlds** and **Document Templates** exposes its label, confirmation prompt, and countdown to assistive technology.
- **Workspace sidebar**: Dragging documents in the hierarchy tree no longer errors in the console when hover auto-expands a closed row for nesting.
- **Workspace sidebar**: You can nest a document under a sibling document row in the same template placement again.
- **Workspace sidebar**: Document rows use a short press-and-hold before drag starts, so quick clicks expand or collapse folders without starting a drag.
- **Workspace sidebar**: Nesting into a document whose children were not yet loaded opens that parent row after the drop so the moved document stays visible.
- **Workspace sidebar**: Expanding a template placement or document row for the first time waits until child rows load before the row opens, which removes flicker from empty placeholder children.
- **Workspace sidebar**: Dragging a document in the hierarchy tree no longer moves it outside its template placement (for example beside the placement row at world level).
- **Workspace**: Open document tabs in the top control strip. Left-click a leaf document in the hierarchy tree to open or focus its tab and navigate to the document workspace route. Middle-click opens a background tab without changing the current page. Each tab shows the template icon and a feather when the document name has unsaved edits. Closing a dirty tab asks whether to discard changes. Tab order and in-progress name edits restore from the **opened_documents** table in the active **`.faproject`** when you reopen the project.
- **Workspace**: The document workspace route opens in preview mode with a read-only document name. **Edit current document** in the control strip switches the tab to edit mode so you can change the name inline. **Save current document** saves and returns to preview; **Save document without exiting edit mode** keeps edit mode open. Save buttons use **primary-bright** when the name matches the saved value and **teal** when you have unsaved edits.
- **Workspace sidebar**: After you save a document display name from the workspace, the matching hierarchy tree row label updates without collapsing expanded worlds or template placements.
- **Workspace sidebar**: Saving a document display name no longer makes already-loaded nested document rows under sibling parent rows disappear until you collapse and reopen the tree.
- **Workspace**: The app control strip keeps a **48px** minimum height so Edit and Save actions stay aligned with the hierarchy **Search...** row.
- **Workspace sidebar**: Reordering nested documents that share the same parent document row in the hierarchy tree works again when you drop on the gap between sibling rows.
- **Workspace sidebar**: Dragging an expanded document subtree to nest under another document no longer collapses open folders about a second after the drop; expanded branches stay open through the move.
- **Workspace sidebar**: The hierarchy tree list has **8px** bottom padding so the last document row is not flush against the panel edge when scrolled to the end.
- **Workspace sidebar**: Dragging documents to reorder within a template placement now saves sibling order to the project database and keeps that order after you quit and reopen the project.
- **Workspace sidebar**: Lazy-loaded document rows nested under a collapsed world, group, or placement stay in saved expand state and reopen once their parent branches load, instead of being forgotten on project reopen or after a document drag.
- **Workspace sidebar**: The hierarchy tree virtual list recalculates scroll height when nested rows expand or collapse, which keeps the bottom of long document lists reachable without a stale scrollbar.

### Known issues
- **Fantasia Archive Settings** still lists many experimental toggles marked as planned work in their titles; those options are not shipped yet and have no effect when enabled.

---

## 2.4.14 - Version bump

### New features
- **Fantasia Archive Settings**: Under **Developer settings**, the **Developer Settings** group includes **Log full activity payload**. When enabled, **Action Monitor** records full action payloads for every activity; when disabled, only non-error and non-warning rows use the default preview cap (errors and warnings always log the full payload).
- **Project Settings**: The **Worlds** tab uses a draggable world list on the left and a detail editor on the right for the selected world's name, optional hex **color**, and remove control. **Add world** inserts a new row at the top of the list. **Save settings** persists the project name and the full worlds list together; at least one world must remain, and worlds that still contain documents cannot be deleted.
- **Project Settings**: Each world's **World color palette** editor supports **Add Color**, drag reorder, and per-swatch color editing; duplicate palette entries show a warning border and block **Save settings** until resolved.
- **Project Settings**: When **Save settings** is blocked, an error icon beside the button lists what must be fixed (blank project name, missing world names, or duplicate palette colors) in a structured tooltip; **General settings**, **Worlds**, and individual world tabs highlight in the negative accent color while those errors remain.
- **Project Settings**: Right-click a palette swatch to open **Duplicate color** or **Delete color** from a context menu; left-click still opens the color picker.
- **Project Settings**: The **World color** field includes a **+** control to add the current hex to that world's palette in the unsaved draft (disabled when the value is empty, invalid, or already listed).
- **Project database**: Creating a **`.faproject`** adds a default **world** whose name matches the **project name**. Each **world** stores a hex **color** and zero-based **sort order**; **Project Settings** snapshot save rewrites the ordered list in one transaction.
- **Project Settings**: Each selected **World** includes a **World template layout** tree (drag reorder, **Add group**, rename/delete groups) and an **Available document templates** list. Click a template to add it at the root; drag templates into groups afterward. **World appendix** copy appears on template rows in both columns. Empty group names block **Save settings**. Right-click a group or placed template row for inline rename in a context menu, or use the **primary-bright** edit control beside remove on each row; both open the rename field anchored to the bottom-left of that row. Group rename edits the group name. Template rename edits an optional per-world **Nickname**; the document template's real name stays on a read-only row in that menu and in **Document Templates**. A non-empty nickname shows in accent color in the layout tree instead of the template name.
- **Project Settings**: **Available document templates** includes a dense search field beside the title that filters the list by document template name or **World appendix**; a clear control appears while text is entered.
- **Project Settings**: On wide screens the dialog grows up to **1800px** wide (other large settings-style dialogs stay at **1400px**), overriding the shared Quasar dialog card width cap for this dialog only.
- **Project Settings**: **Worlds** and **Document Templates** tab lists each include a pinned dense search field above the scrollable tab column that filters by world name or by document template name plus **World appendix**; the field spans the tab column width with 45px horizontal padding, and a clear control appears while text is entered. The **Document Templates** filter row also has 10px padding below the field. Drag reorder stays enabled while filtering; reordering visible matches updates order in the full list.
- **Project database**: Per-world template layout persists in **`world_template_groups`** and **`world_template_placements`** (schema **v7**), saved atomically with the worlds snapshot. Placement rows may store an optional **nickname** per world. Legacy **`world_document_templates`** junction rows migrate to root placements on upgrade.
- **Open Project Settings**: With an active **`.faproject`**, the default global shortcut **Ctrl+Alt+Shift+P** on every desktop platform (literal Control on macOS) opens **Project Settings**, matching **Project Management → Project Settings** and listing **Open Project Settings** in **Keybind Settings**.
- **Project Settings**: **Document Templates** stores each template title per interface language. The **Document template name** field shows the resolved title for your current language (read-only); use **Document title translations** beside that field to edit titles for every supported language. New templates pre-fill only the active UI language. Display elsewhere falls back current language, then **en-US**, then other locales alphabetically. At least one non-empty translation is required before **Save settings**.
- **Project database**: Document templates persist per-locale titles in **`title_translations_json`** (schema **v8**); existing **`display_name`** values migrate to **en-US** on upgrade.
- **Project Settings**: **World name** and document template **World appendix** are stored per interface language. Each field shows the resolved text for your current language (read-only); use the translate control beside the field to edit every supported language. New worlds and templates pre-fill only the active UI language. Display elsewhere falls back current language, then **en-US**, then other locales alphabetically. At least one non-empty world name translation is required before **Save settings** (same rule as document template titles).
- **Project database**: Worlds and document templates persist per-locale names and appendix copy in **`display_name_translations_json`** and **`world_appendix_translations_json`** (schema **v9**); existing single-locale values migrate to **en-US** on upgrade.
- **Project Settings**: Document template tabs show each template's **Icon** beside the name (default outline file icon when none is chosen); the icon stays vertically centered on the title when a **World appendix** line appears below.
- **Project Settings**: Vertical tab labels in **Project Settings** (including **Worlds** and **Document Templates**) use 14px type with tighter line height for denser lists.
- **Project Settings**: **World template layout** group names and placement **Nickname** values are stored per interface language. Group rename and nickname rename menus use the same translate control as **World name** and document template titles. Nickname rename shows the resolved document template title for your current language pinned beside the locale list (read-only). Empty nickname in the active language shows the template title in the tree. At least one non-empty group name translation is required before **Save settings**.
- **Project database**: Per-world template layout persists per-locale group labels and placement nicknames in **`display_name_translations_json`** on **`world_template_groups`** and **`nickname_translations_json`** on **`world_template_placements`** (schema **v10**); existing single-locale values migrate to **en-US** on upgrade.
- **FaLocaleTranslationsInput**: Optional pinned-aside column in the translations menu shows a read-only reference value (for example canonical template title) while locale rows scroll independently on the right.
- **Project Settings**: **Worlds** and **Document Templates** tab rows each show a warning icon when the active interface language is missing a translated **World name** or document template title, with a tooltip explaining the gap.
- **Project Settings**: Document template **Document template name** and per-world placement **Nickname** rename menus use **Singular** and **Plural** columns per interface language. Display falls back plural, then singular, within each locale, then across locales. At least one non-empty **plural** title remains required before **Save settings**; singular forms are optional.
- **Project database**: Document templates and placement nicknames persist optional per-locale singular forms in **`title_singular_translations_json`** and **`nickname_singular_translations_json`** (schema **v11**); existing **`title_translations_json`** and **`nickname_translations_json`** remain plural storage with no data copy on upgrade.
- **FaLocaleTranslationsInput**: **`translationForms="singularPlural"`** shows **Singular** and **Plural** columns per locale (wider menu). Summary field uses the combined plural-then-singular resolver for the active language.
- **Project Settings**: Missing-translation warning tooltips for document template tabs, **Available document templates**, and **World template layout** template rows now list which singular and/or plural forms are missing for the active language and name the fallback locale when another language supplies the shown label.
- **Project Settings**: **Save without closing** persists project settings like **Save settings** but keeps the dialog open so you can keep editing.

### Bugfixes & Optimizations
- **Localization**: Action manager save failures, project open/create error fallbacks, and **Import / Export App Configuration** error toasts now use locale files in every supported language. **Fantasia Archive Settings** **Log full activity payload** help text matches English in all locales.
- **Action Monitor**: Failed and warning activities always store the full action payload in session history; **Log full activity payload** now applies only to successful and in-progress rows.
- **Project Settings**: **World's hierarchical tree** and **Available document templates** now show document template titles and **World appendix** text for the active interface language when translations exist, including after opening the dialog or switching language while it stays open. **Available document templates** rows also show the missing-translation warning icon when the active language has no document template title translation.
- **Project Management**: menu rows regrouped — **Create new project**; **Load existing project** and **Load Recent Project**; **Show Project Dashboard**; **Toggle Project Noteboard**, **Custom Project CSS**, and **Project Settings**; **Advanced Project Tools** — with separators between each group.
- **Project Settings**: **Worlds** and **Document Templates** tab lists hide the vertical scrollbar on the tab column while the list remains scrollable; **Add world** and **Add document template** stay pinned below the scroll area.
- **Project Settings**: **World's hierarchical tree**, **Document Templates** tab list, and **Worlds** tab list scroll to reveal the newly appended row when you add a group, place a template from **Available document templates**, add a document template, or add a world.
- **Project Settings**: Dragging rows in **World's hierarchical tree** no longer resets scroll to the top after reorder when the list is scrolled; adding groups, templates, or worlds still scrolls the tree to show the new row.
- **Project Settings**: **Available document templates** rows keep hover highlight; right-click no longer leaves a row highlighted after the pointer leaves.
- **Theme**: The **negative** accent color (errors, delete controls, validation highlights) is now **#ff4040**.
- **Tooltips**: hover tooltips across the app now open after a consistent **0.5 s** delay. The per-world nickname row tooltip in **World's hierarchical tree** follows the same delay when opened programmatically.
- **Project Settings**: Template rows with a per-world nickname in **World's hierarchical tree** show a right-side hover tooltip with **Nickname** and **Original name** lines across the full row height; edit and remove button tooltips take precedence, and the nickname tooltip returns when the pointer moves back over the row label or padding.
- **Project Settings**: **World template layout** group and nickname rename menus open the locale translation list immediately (no intermediate read-only summary field).
- **FaLocaleTranslationsInput**: Pinned-aside nickname rename menus lay out canonical template title and locale rows side by side without overlapping labels at the top.
- **FaLocaleTranslationsInput**: Pinned-aside rename menus no longer show an unintended horizontal scrollbar at the bottom of the popup.
- **FaLocaleTranslationsInput**: **`translationForms="singularPlural"`** menus keep at least **650** pixels wide so **Singular** and **Plural** columns stay readable; locale labels stack above each input inside a vertical scrollbar.
- **FaLocaleTranslationsInput**: Pinned-aside rename menus align the read-only reference column with the first locale input row instead of the **Singular** / **Plural** header row.
- **Tooltips**: Missing-translation and spellcheck-reload help tooltips that use line breaks now render each line on its own row instead of collapsing into one line.
- **Project Settings**: **Delete world** in the **Worlds** detail row aligns vertically with the **World name** and **World color** fields.
- **Language selector**: **Svenska** now shows the Sweden flag instead of the El Salvador flag.
- **Project Settings**: Hovering edit or remove on nickname template rows in **World's hierarchical tree** shows only the button tooltip, not the nickname and original-name popup.
- **Project Settings**: Nickname hover tooltip in **World's hierarchical tree** again covers the full row height, including top and bottom padding beside the label.
- **Project Settings**: Nickname hover tooltip in **World's hierarchical tree** stays hidden while the inline rename menu is open.
- **Project Settings**: Help tooltips beside **Nickname inside this world** and **Document template name** in the inline rename menu open to the right of the question-mark icon.
- **Icon picker**: The selected icon in the grid uses the **primary-bright** accent instead of primary.
- **Project Settings**: Template edit tooltip in **World's hierarchical tree** reads **Adjust template's nickname** instead of **Rename nickname**.
- **Project Settings**: Group rename in **World's hierarchical tree** shows a **Name of the group** label on the inline rename field.
- **Project Settings**: Inline rename fields in **World's hierarchical tree** (**Name of the group**, **Nickname inside this world**, **Document template name**) keep stacked labels above the input text.
- **Project Settings**: Inline rename in **World's hierarchical tree** focuses the rename field every time the menu opens, including when switching between rows.
- **Project Settings**: The per-world layout column title is **World's hierarchical tree** (replacing **World template layout**).
- **Project Settings**: **Save settings** shows a success toast only after project name, worlds, and document templates all persist; database or bridge failures surface an error toast and keep the dialog open. Each successful save is recorded in **Action Monitor** as **saveProjectSettings**.
- **Project Settings**: The **World appendix** field in **Document Templates** is capped to a narrower width so long appendix names do not stretch the detail column.
- **Project Settings**: Opening **Worlds** no longer fails with a console error when loading worlds from the active **`.faproject`**; project-content IPC now returns the worlds list payload the dialog expects.
- **Project Settings**: **World color** picker footer swatches list only the active world's palette instead of every world's colors.
- **Project Settings**: World tabs select reliably on click even when the pointer moves slightly before release; drag reorder uses a small movement threshold so short clicks are not treated as drags.
- **Project Settings**: The **+** control beside **World color** shows a tooltip when the current hex is already in that world's palette.
- **Project Settings**: **World color** and palette color pickers show only the spectrum and palette tabs (the **Tune** sliders view is hidden).
- **Project Settings**: The **World color palette** help tooltip lists right-click **Deletion** and **Duplication** on separate lines.
- **Project Settings**: **World template layout** drag-and-drop commits reliably after each drop (single scheduled commit, cancel cleanup, stale nested-layout guard) so **Add group** after nesting no longer flattens templates to the root.
- **Project Settings**: Duplicate assignments of the same document template in one world's layout block **Save settings**, show save-error tooltip text, and highlight the **Worlds** tab, world tab, and duplicate template rows in the negative accent color until one placement is removed.
- **Project Settings**: A blank document template name in **Document Templates** highlights every world sidebar tab that still places that template and the **Worlds** category tab until the name is filled in; **Save settings** stays disabled while that error remains.
- **Welcome screen**: **Resume Latest Project** again shows the action manager failure toast when the recent project cannot be opened (for example a missing **`.faproject`** file), instead of failing silently after a console error.
- **Project load failures**: When **loadExistingProject** fails, the error toast caption shows only the **`.faproject`** path that could not be opened (including **Load existing project** after the file picker); technical backend messages stay in the console and **Action Monitor** history.
- **Desktop development**: **`yarn install`** now runs **`electron-builder install-app-deps`** so **better-sqlite3** native bindings match the bundled **Electron** runtime; use **`yarn rebuild:native`** after changing **Electron** or **Node** if project open still reports missing bindings.
- **Global shortcuts**: Fantasia Archive no longer intercepts or blocks its reserved **Ctrl+Shift** keyboard shortcuts in other applications while its window is in the background. The app-wide shortcut handlers now register only while the **Fantasia Archive** window is focused and release the moment it loses focus or closes, so those key combinations stay available to other programs whenever Fantasia Archive is not the active window.
- **Project Settings**: Adding a document template pre-fills both singular and plural title for the active interface language, so a new tab no longer shows a missing-translation warning before you edit titles.
- **Project Settings**: **Save settings**, **Save without closing**, and the save validation error icon sit in one footer row with consistent spacing so button labels stay on one line on wide layouts.

---

## 2.4.13 - Version bump

### New features
- **Interface languages**: The language selector lists **16** interface languages (**English, US**, **Deutsch**, **Français**, **Italiano**, **Svenska**, **Suomi**, **Norsk**, **Português**, **Español**, **Ελληνικά**, **Українська**, **العربية**, **中文**, **日本語**, **हिन्दी**, and **Русский**) in Latin alphabetical order by each language’s endonym. Menu labels mark AI-assisted locales with a localized **AI** suffix (the letters **AI** stay Latin in every label).
- **Translations**: Every selector language ships full UI strings for menus, dialogs, floating windows, notifications, and in-app help markdown (**Advanced Search Guide**, **Advanced Search Cheat Sheet**, **Tips, Tricks & Trivia**, and the **Changelog**), generated from the corrected **English, US** catalog.
- **Arabic layout**: Choosing **العربية** applies right-to-left layout across the app; the language menu stays left-to-right with per-row reading direction so endonyms display naturally.
- **Desktop installers** register the **`.faproject`** type for **Open with** on **Windows**, **macOS**, and **Linux** packages; double-clicking a project file or choosing **Fantasia Archive** in the system picker opens that **`.faproject`** as the active session and updates **Load recent project** when the open succeeds (development **`yarn quasar:dev:electron`** skips automatic opens unless you set **`FA_DEV_OPEN_FAPROJECT=1`**).
- **Welcome screen** and **Project Management**: The splash welcome actions and matching **Project Management** rows use **Create new project** and **Load existing project** (**en-US**, **Deutsch**, **Français**).
- **Welcome screen**: When **Project Management** has a saved **Load recent project** list, **Splash** shows a split resume control: the main segment reads **Resume Latest Project** when no **`.faproject`** is active (opens the newest recent file) or **Resume Current Project** when a session is already loaded (returns to that file without an already-active warning). The arrow lists the same recent-project rows as the menu (name plus path) and exposes **Browse latest projects** as its accessible name and a **Quasar** tooltip to its right. With no saved recents, that control stays hidden.
- **App Settings**: **Hide "Browse latest projects" tooltip** removes the caret tooltip beside **Resume Latest Project** on the welcome screen; the splash updates when you save the toggle without restarting the app.
- **Custom Project CSS**: With an active **`.faproject`**, **Settings & Tools** lists **Custom Project CSS** beside **Custom App CSS**. The movable Monaco window persists per-project stylesheet text into SQLite with debounced autosave, applies live preview through **`#faProjectUserCss`** after app-wide **Custom App CSS**, keeps the floating frame between **Custom App CSS** and the scratch noteboards, and refreshes when you load, switch, or import projects. The default global shortcut is **Ctrl+Shift+J** on every desktop platform (same literal Control key on macOS), wired as open-only: it never closes the window, and a repeat press while the editor stays open leaves your in-memory draft untouched.
- **Project Noteboard**: **Project Management** gains **Toggle Project Noteboard**, disabled until a **`.faproject`** is loaded, default shortcut **Ctrl+Alt+D** on every desktop platform; **Close** shows the muted parenthesized shortcut under the label like **App Noteboard**. Text and movable window geometry persist per project in SQLite **`project_data`** KV rows; the renderer keeps that floater stacked above **App Noteboard** when both are open while both stay below modal chrome (**6000+**).
- **Project Settings**: **Project Management** lists **Project Settings** when a **`.faproject`** is loaded. The dialog matches **Fantasia Archive Settings** layout (vertical tabs, **Close without saving**, **Save settings**) without a search field. **General settings** edits the **Project Name** stored in SQLite; saving updates the workspace header, **Load recent project**, and splash **Resume Latest Project** lists immediately and persists across relaunch.
- **Workspace home**: The **`/home`** route shows a **Project overview for** heading, the active project name, and a wide **Did you know?** tips card with a random line from **Tips, Tricks & Trivia** (respecting **Hide project overview tips & tricks** and **Hide plushes** in **App Settings**).
- **Show Project Dashboard**: **Project Management** lists **Show Project Dashboard** when a **`.faproject`** is loaded. The default global shortcut is **Ctrl+Shift+Alt+O** on every desktop platform (literal Control on macOS).

### Bugfixes & Optimizations
- Bumped **axios** to **1.16.1** for HTTP and fetch adapter fixes (redirect and proxy **Host** handling, fetch **maxBodyLength** / **maxContentLength** enforcement) and security hardening from recent **1.15.x** and **1.16.x** releases.
- **Contributor layout**: Repository-root automation moved from **`scripts/`** to **`.utility-scripts/`** so it is distinct from **`src/scripts/`** application code and from **`package.json` `scripts`**; **README**, **AGENTS**, Cursor rules/skills, and **`yarn`** commands that invoke those **`.mjs`** files use the new path.
- **Contributor layout**: **Electron** **`mainScripts`** areas now follow the same two-level layout as the renderer (**`functions/`**, **`*_manager.ts`** entry modules, **`*Wiring.ts`** implementation siblings); cross-area imports prefer those managers (for example **`projectManagement_manager`**, **`appConfig_manager`**, **`windowManagement_manager`**, **`ipcManagement_manager`** with **`registerAllFaIpc`**).
- **Action Monitor**: Failed desktop-bridge refreshes for **Keybind Settings**, **Custom App CSS**, and **Custom Project CSS** at startup or before those surfaces open now surface one shared error toast through the **action manager** instead of duplicate notifications from each store.
- **Global keyboard shortcuts**: On desktop builds, Electron registers app **globalShortcut** handlers and blocks Chromium-reserved literal **Ctrl+Shift+** letter and **Delete** shortcuts (twelve keys), then forwards them to the renderer with the correct DOM key **code** so Fantasia defaults, **Keybind Settings** capture, and user overrides work instead of opening the browser bookmarks manager, clear-data dialog, or similar built-ins.
- **App Settings**: **Hide project overview tips & tricks** updates the workspace home **Did you know?** card while **Fantasia Archive Settings** stays open (live preview); **Close without saving** restores the prior value.
- **Workspace shell**: On **`/home`**, the **Essential links** drawer stays in desktop mode without a dimming overlay so workspace content keeps the correct inset beside the panel while the drawer opens and when the window is resized narrower.
- **Storybook**: **Project overview**, **MainLayout** home, and **Index page** canvas stories seed an active project and tips copy; **Tips, Tricks & Trivia** document mocks run through the same **`@`** / **`|`** escaping as the app so markdown dialogs render in the canvas; **Essential links** mock bullets match production.
- **Top bar menus**: Unimplemented **Documents & Content** rows (**Quick-Add New Document**, **Quick-Search Existing Document**, **Mass Delete Documents**, **Export Project / Documents**) stay hidden until those flows ship. **Project Management** likewise hides the **Merge Another Project into the Current One** and **Convert Legacy Project to FA 2.0** rows under **Advanced Project Tools** until those actions exist; implemented project-scoped rows still follow the active **`.faproject`** session rules.
- **Import / Export App Configuration**: **Create export file** again records both the export package step and the OS save outcome as separate **Action Monitor** rows when the save dialog completes.
- **Project Settings**: **Icon picker** trigger and icon grid show Font Awesome and brand icons at the same fixed size as document-template tab icons instead of filling the whole button.
- **Project Settings**: Document-template tab icons and rows in **Available document templates** and **World's hierarchical tree** scale wide Font Awesome glyphs inside a fixed icon box so brand and solid icons no longer overflow their column.
- **Project Settings**: Document-template icon columns in the available-templates list and layout tree use a wider leading indent so icons align with row labels and **World appendix** lines on document-template tabs.
- **Project Settings**: Default Material Design outline icons in document-template lists and the **Icon picker** grid match Font Awesome icon scale inside the shared fixed icon box.
- **Icon picker**: The trigger button shows the selected icon slightly larger than list icons for easier preview; the grid and document-template tab rows stay unchanged.
- **Project Settings**: **Save settings** writes document templates before the worlds snapshot when both change in one save, preventing a database error when world template placements reference templates being saved in the same session.
- **Developer tools**: **Help & Info → Toggle Developer Tools** and the default **F12** shortcut open Chromium DevTools again in packaged desktop builds.
- **English copy**: **App Settings** option titles and descriptions, import/export guidance, keybind and action-monitor labels, and help markdown were proofread in **English, US**; staging **TODO** prefixes were removed from setting labels.
- **Interface languages**: The language selector lists **English, US** without the **AI** suffix because it is the source catalog, not an AI-assisted locale.
- **Social contact**: Welcome-screen social buttons now refresh their labels when you change the interface language instead of staying on the language active at app start.
- **Welcome screen**: **`/`** and **`/home`** now share one **MainLayout** shell (top-bar menus, language selector, and window controls stay mounted). Only the center page crossfades between the welcome screen and workspace; the left drawer slides in on **`/home`** with the same timing. When a **`.faproject`** session becomes active — create, load, resume, recent-project picks, or opening a project from the OS — the app navigates to **`/home`** with a short fade transition from any non-workspace route (welcome, **Error / Not found**, or future **MainLayout** pages), except the **component testing** harness route.
- **Welcome screen**: The left **Essential links** drawer stays fully off the welcome route — no reserved rail and no drawer chrome on **`/`** — and slides in on **`/home`** when the workspace opens (same timing as the page crossfade) instead of popping in at full width.
- **Error / Not found**: Unknown routes now use the same **MainLayout** top bar as the welcome and workspace screens (**Project Management**, **Documents & Content**, **Settings & Tools**, **Help & Info**, plus the language selector and window controls) without the left drawer.
- **Error / Not found**: When a **`.faproject`** session is already active, the page shows **Resume Current Project** above **Return to app start screen**; it reopens the active file the same way as the welcome **Resume Current Project** primary segment, without the recent-project dropdown or an already-active warning toast.
- **Project loading**: Re-opening the project that is already active — from **Load existing project**, **Load recent project**, OS double-click, or matching menu or splash **Load existing project** rows — shows a **warning** toast in the same **project name** line format as a successful load, does not reload the database, and navigates to the workspace when you were on the welcome screen or **Error / Not found** page. **Resume Current Project** on **Error / Not found** and the welcome resume primary segment (**Resume Current Project** when a session is active, **Resume Latest Project** when none) return to the workspace quietly with no warning.
- **Welcome screen**: The **Resume Latest Project** recent-project menu uses the same slate page background, gold border, bright gold row titles, muted paths, gold row dividers, a light primary-bright hover tint, and the same drop shadow as top-bar menus instead of the default dark teal app menu panel.
- **Welcome screen**: The split **Resume Latest Project** dropdown arrow shows its chevron again; inherited uppercase text styling had broken Material Icons ligatures inside that segment.
- **Welcome screen**: **Create new project** on the welcome route opens **New Project** through the same **AppControlMenus** dialog host as the workspace route.
- **App Settings**: **Hide welcome screen social links** hides the gold divider and **Social contact** button rows on the welcome screen; the splash reacts when saved settings change in **App Settings**.
- **App Settings**: **Hide tips popup on start screen** suppresses the startup **Did you know?** tips notification on the welcome route (read once when the app boots, not on every settings change).
- **App Settings**: **Skip welcome screen** opens the latest recent **`.faproject`** on launch — or the active session file when one is already loaded — and navigates to the workspace (**`/home`**, shared **MainLayout** with the left drawer) instead of staying on the welcome screen; the splash also redirects when you turn the setting on while the welcome route is showing. If that newest recent file no longer exists, the app shows an error and stays on the welcome screen instead of opening the next recent project (including after boot already tried skip once); with no recent projects, the welcome screen still appears.
- **App startup**: **Skip welcome screen** auto-load now mounts the welcome shell first, waits for the desktop bridge and saved settings, runs the MRU open attempt, then lets **MainLayout** refresh recent projects so a missing MRU head is not silently dropped before the skip logic runs.
- **Custom App CSS** and **App Noteboard**: Reopening either floating window after relaunch restores the last saved position and size instead of opening at the default centered layout while startup settings and styling data are still loading.
- **Project Management**: Dropped **Save Project** and **Close Project** from the menu until those actions have real desktop implementations (**en-US**, **Deutsch**, **Français** copies updated).
- **Project SQLite**: Main keeps the active **`.faproject`** path beside the database handle so brief desyncs (for example after the window idles) can reopen the same file, retry the operation once, and optionally ask the renderer for the path when main lost both. Full renderer reloads still clear the session on real document navigations, but same-document and subframe churn no longer drops the handle alone.
- **OS-open handshake**: Splash boot **`faProjectOsOpen`** now waits briefly for **`faContentBridgeAPIs.faProjectOsOpen`** before the **renderer-ready** IPC handshake so launching with a **`.faproject`** path on Electron **argv** (installer associations or **FA_E2E_OS_OPEN** end-to-end replay) cannot miss wiring when preload **`contextBridge`** surfaces trail the first boot ticks.
- **Custom Project CSS**: Saving from the floating editor when no **`.faproject`** session is active (for example on the welcome screen or after the app cleared the database handle) now shows a clear **Custom Project CSS** message instead of treating a skipped SQLite write as success and then surfacing a mismatch error.
- **Project Noteboard**: loading another **`.faproject`** while the window stays open now moves and resizes the frame to match that project’s saved geometry rather than leaving the prior project’s footprint on screen.
- **Project Noteboard**: moving or resizing the window no longer clears scratch text on screen when a frame-only save round trip read SQLite before the debounced text write finished; the renderer keeps the live textarea value when merging read-backs for patches that omit **text**.
- **Project Noteboard**: saving note text or geometry no longer triggers an error toast when **main** has cleared the SQLite handle during a renderer reload (for example **Control+F5** or the language spellcheck refresh): the deferred write drops quietly instead of implying a failing save round trip.
- **Storybook**: The nested workspace **content bridge** mock now stubs **project noteboard** plus **project styling** KV read and write so **WindowProjectNoteboard** and **WindowProjectStyling** **Autodocs** plus canvas previews no longer show a spurious **action manager** failure when those floating windows autosave geometry, note text, or CSS drafts. A **mocks**-scoped **TypeScript** config restores **`app/`** path typing for maintainers without widening the typecheck to the whole preview bundle.
- **Storybook**: **AppControlMenus** Autodocs component description lists the current **Project Management** rows so it matches **`_data/project`** after menu changes.
- **Tests** — End-to-end **`checkProjectStyling`** renames **`.faproject`** fixture baselines and suite titles so the **Custom Project CSS** flow describes Monaco live preview, SQLite persistence, clearing the stylesheet, and per-project isolation instead of opaque **alpha** / **beta** labels. Component harness **`WindowProjectStyling.playwright.test`** covers floating chrome, cold-loaded **Monaco**, the help panel theme list, and **Close without saving** leaving stored project CSS empty.
- **Tests** — **`checkProjectManagementFlow`** end-to-end now asserts splash **Resume Latest Project** (**MRU dropdown labels** match **Project → Load recent project**, reload through the split primary segment after the spellcheck session reset, load from another recent row).
- **Tests** — **`checkProjectOsOpenArgv`** replays packaged **electron.launch argv** cold opens with **`FA_E2E_OS_OPEN=1`** isolated from ordinary splash-only launches.

---

## 2.4.12 - Version bump

### New features
- **Top bar menus** (**English**): The four primary **`AppControlMenus`** **`title`** strings read **Project Management**, **Documents & Content**, **Settings & Tools**, and **Help & Info** (replacing earlier short **Project** / **Documents** / **Tools & settings** / **Help & info**, then interim longer wording). Submenu rows use headline-style capitalization on principal words (**en-US**) under those titles. **Deutsch** and **Français** top titles match the same four roles.
- **App-level wording**: User-facing labels and documentation for the scratch **noteboard** (styled **noteboard**, one word), **App Settings**, **Custom App CSS**, and **Import / Export App Configuration** now use consistent **app** vocabulary (internal identifiers, IPC channel names, and paths were aligned). **Documents & Content** lists **Quick-Add New Document**, **Quick-Search Existing Document**, **Mass Delete Documents**, and **Export Project / Documents**. **Settings & Tools** lists **Toggle App Noteboard**, **Custom App CSS**, **Keybind Settings**, **App Settings**, then **Import / Export App Configuration** (below a separator). **electron-store** and **.faconfig** ZIP entries use **`faAppNoteboard.json`** and **`faAppStyling.json`** only.
- **App Noteboard**: **Close** stacks the muted, parenthesized **Toggle App Noteboard** chord under the label using the same keybind hint color and compact type size as shortcut lines on **Settings & Tools** menu rows. Stripped-down component-testing launches load persisted keybinds when that layout mounts so harness previews resolve the hint like the normal shell.
- **Welcome screen** — the app opens to the default route with the same top menus, language control, and window buttons as the rest of the app; the left navigation drawer stays hidden on that screen only (**`faMainLayoutHideDrawer`** route **meta**). The centered body matches classic **FA 1.0** (welcome line and title with mirrored wing art, three gold-outlined primary actions with no behavior yet, a divider, then the current **Social contact** button row). The welcome line, wing **img** pair, and actions live in **`SplashControls`** (**`src/components/other/SplashControls/`**, layout tokens in **`styles/_variables.scss`**); **`SplashPage`** keeps the page shell, divider, and **`SocialContactButtons`**. Wing art uses **`public/images/splash/splash_logo.png`** (aligned with the **512** favicon). **`index.html`** preloads that **PNG** with high fetch priority; each wing **img** uses **loading=eager** so the art is requested for first paint. The dev scratch **Index** page remains at **`/home`** with the same shell including the drawer.
- **Active project**: when **`S_FaActiveProject`** has no loaded project, **Project Management** disables **Save Project**, **Show Project Dashboard**, **Project Settings**, **Close Project**, and **Merge Another Project into the Current One** (under **Advanced Project Tools**); **Documents & Content** disables **Quick-Add New Document**, **Quick-Search Existing Document**, **Mass Delete Documents**, and **Export Project / Documents**. **Settings & Tools** stays fully available, including **Import / Export App Configuration** as its last row below **App Settings**. **New Project**, **Load Project**, **Advanced Project Tools** (parent row), **Convert Legacy Project to FA 2.0**, and **Toggle App Noteboard** stay available.
- **New project** — **Welcome** screen **New project** and **Project Management → New project** open a persistent settings dialog, then create a **`.faproject`** SQLite file via the main process (**Splash** and **Project Management** wired through **`faActionManager`**). The header shows the active project name when one is loaded. When creation succeeds, **`faActionManager`** shows one positive **Notify**: **New project created and ready to go!** with the project name on the same line (the dialog does not send a second toast).
- **Load project** — **Welcome** screen **Load project** and **Project Management → Load project** open an OS file picker that defaults to **Downloads** (Playwright **E2E** uses isolated **userData** instead of the native dialog). Choosing a **`.faproject`** opens it in the main-process SQLite handle and updates **`S_FaActiveProject`** like **New project**. One positive **Notify** confirms load with **Project loaded and ready to go!** and the project name on the same line.
- **Load recent project** — **Project Management** includes **Load recent project** (after **Load project**) with up to **10** recently opened or created **`.faproject`** files. Entries come from the desktop shell’s saved list; missing files are dropped automatically. Choosing a row opens that path the same way as **Load project** and moves it to the top of the list when the open succeeds; opens by path that fail remove that row from the list except when the project is already active.
- **Quasar** public brand variables on **`:root`**, from **`--q-primary`** through **`--q-dark-page`**, are re-bound to the matching **Fantasia** **`--fa-color-…`** names in **`src/css/fa-quasar-public-vars-bridge.scss`** (last **app** stylesheet) so **Custom App CSS** can drive **`var(--q-*)`** from the same Fantasia color token set.
- **SCSS** theme layout: **`app.palette.scss`** (including semantic text tokens under **APP COLORS**), default custom properties in **`fa-theme.scss`**, shared **`faSemanticText.scss`** ( **`fa-text-*`**, **`fa-icon-*`**, and **`.text-white` / `.text-black`** mapped to **Fantasia** neutrals), split **`q-*`** tokens under **`src/css/theme/quasar-components/`**, colocated feature **`styles/_variables.scss`**, globals **`htmlAdjustments.variables.scss`** and **`scrollbar.variables.scss`**, and a slim **`quasar.variables.scss`** barrel. **`yarn audit:quasar-component-tokens`** helps catch missing **`$q*`** definitions.
- **UI copy and icons** use those helpers instead of ad hoc **Quasar** Material text shade classes where a role should follow runtime theme. **Notify** defaults no longer set **`textColor`**.
- **Custom App CSS** — the **?** help menu (for example under **Fantasia Archive variable list**) lists **`--fa-color-…`** names with a small color swatch to the left of each name (the swatch uses **`var(…)`** from **`:root`**, so it matches your theme and **Custom App CSS**). When live styles are present (including **Vite** HMR in dev), names come from the **DOM**; otherwise the list falls back to the same names parsed from the bundled **`fa-theme.scss`** text. The renderer no longer depends on **Node** file reads for that panel.
- **Custom App CSS** — **Editor keybinds** help (**?** in the title row): a **`QMenu`** styled like the global Fantasia **QTooltip** replaces the old hover-only tooltip. It opens after the same **500 ms** hover delay or on a help-icon click, stays open for reading until you click outside the panel, press **Escape**, or click the help icon again; **`no-focus`** keeps **Monaco** from blurring when the menu opens.
- **Top bar**: **Quick-Add New Document**, **Quick-Search Existing Document**, and **Mass Delete Documents** moved from **Settings & Tools** into **Documents & Content** (same actions and labels); **Documents & Content** ends with **Export Project / Documents**. **Project Management** no longer lists **Export Project / Documents** (same action lives only under **Documents & Content**). **Import / Export App Configuration** is the last row in **Settings & Tools**, below **App Settings** and a separator.
- **Top bar menus**: nested submenu panels (for example **Project Management → Load Recent Project** and **Advanced Project Tools**) open when you point at the parent row, with a short delay before they close so the pointer can move into the floating panel; clicking the row still toggles the submenu where **Quasar** supports it alongside controlled visibility.
- **Help & Info**: **About Fantasia Archive** appears above **Changelog** (before **License**).

### Bugfixes & Optimizations
- **Custom App CSS**: **Quasar** dark **`q-field`** underlines, input copy, labels, and standout highlighted or readonly borders now use **`var(--fa-color-white)`** or **`color-mix`** against it instead of fixed **`#fff` / RGBA**. Dark **`q-toggle`** idle thumb discs, inner label stack, and on-state thumb icon tint follow **`--fa-color-white`** the same way.
- **Welcome / About social rows**: **Social** chip labels and fills use **Sass** palette tokens only (no **`fa-v()`** or runtime **`--fa-color-*`** wiring in those component variable files), so **Custom App CSS** cannot retint **Patreon**, **Ko-fi**, **GitHub**, **Discord**, **reddit**, **Website**, or **X** branding. **Discord**, **reddit**, and **GitHub** label colors are pinned on the **`q-btn`** root with **`!important`** so inherited **Quasar** dark text rules do not override them.
- **Custom App CSS**: Overrides for Fantasia **`--fa-color-dark`** (and matching brand tokens on **body**) now reach **Quasar** **`.q-dark`** backgrounds that read **`var(--q-dark)`**. The stylesheet bridge repeats the **`--q-*`** custom property bindings on **body**, not only on **`:root`**, because inherited CSS variables carry their computed value; **body-level** **`--fa-color-*`** edits would otherwise fail to repaint dark **Quasar** surfaces. **`.q-dark`**, **`body.body--dark`**, **`.q-layout__section--marginal`**, and dark **`.q-list` / `.q-item`** chrome **use** **`var(--fa-color-white)`** or **`color-mix`** against **`--fa-color-white`** instead of fixed near-white values, so **`--fa-color-white`** retints those stacks together with **`--fa-color-dark`** / **`--q-primary`** fills.
- **English copy**: **en-US** **running prose** (dialog bodies, paragraphs, **`Notify`** message lines, changelog narrative) stays sentence-style (first word capped, lowercase later words unless acronyms or product names (**CSS**, **FA**, Fantasia Archive, **FA 1.0**, Electron, Monaco, SQLite, UI, **`macOS`**, etc.). **Chrome** (**top-bar** submenu rows, **Splash** primary actions keyed to menus, **`dialogs.keybindSettings.commands.*`**, **`Action Monitor`** column headers plus matching **`globalFunctionality.faActionManager`** labels, checklist captions beside toggles, principal **dialog** and **floating-window** headings such as **Fantasia Archive Settings**) uses headline-style capitalization on principal words while short grammatical words stay lowercase where conventional (**merge … into the current one**, **Tips, Tricks & Trivia**). Markdown-backed dialog headings keep **Advanced Search Guide** and **Tips, Tricks & Trivia** spelling. The four **top bar** **`title`** bars stay richer title casing (see **New features** above). **French** mirrors headline casing on comparable rows (**Astuces, conseils & anecdotes**); **German** keeps ordinary noun capitalization and was not rewritten mechanically.
- **Desktop persistence**: **electron-store** no longer auto-renames legacy desktop JSON files under **userData**; **.faconfig** import accepts only the current **`faApp*.json`** basenames inside the archive; unknown keybind override keys (including retired ids) are dropped on cleanup instead of being rewritten to new ids.
- **Wording**: Residual application-level labels were normalized to **app** vocabulary (for example **Custom App CSS** titles in **en-US** / **de** / **fr**, tips trivia, import archive validation copy, noteboard bridge errors, and matching contributor docs or test titles).
- **Dialog and floating-window titles** use **Quasar** **text-h5** on semantic **`h5`** headings (**About Fantasia Archive**, **Action Monitor**, **Keybind Settings**, **Fantasia Archive Settings**, **Import / Export App Configuration**, **New Project**, **Custom App CSS**, **App Noteboard**) so headers match a single compact scale.
- **App Noteboard**: the editor area uses the same **`dark-lighter`** panel background as **Settings & Tools** menu rows; **Close** is centered in the bottom action row. The note field is a native **textarea** so content scrolls inside the window when it is small; native spell checking is off, with **`autocorrect`** / **`autocomplete`** hints off and a **Grammarly** opt-out attribute for scratch text. The **`h5`** title uses the same vertical margin token as **Custom App CSS** so both floating-window headers line up.
- **Import / Export App Configuration**: **Export** and **Import** checklist panels are each wrapped as their own **Quasar** **QStep** children of the dialog **QStepper**, so choosing **Export** or advancing to import selection shows the correct step content instead of only the first panel registering with the stepper. The stepper body keeps a minimum height and no longer uses the horizontal slide animation so the checklist stays visible in packaged **Electron** builds where the previous flex and transition styling could leave the content area collapsed or clipped after switching steps.
- **Import / Export App Configuration**: **Apply import** from a **.faconfig** archive refreshes **Custom App CSS** in **Monaco** while that floating window stays open so the editor matches freshly imported styling, the same behavior as the scratch **noteboard** staying aligned with persisted text when it stays open.
- **Top bar menus**: the first row in each open menu or submenu no longer shows the thin divider line above it; thin dividers still appear between consecutive rows and full separator rows behave as before.
- **Settings & Tools** menu no longer lists **Toggle Hierarchical Tree** as a menu row.
- **Maintenance**: Removed an unused keybind chord label re-export, stale commented placeholders at the end of the **Electron** main entry file from early SQLite bring-up, and a redundant **Help & Info** menu TODO in menu data (no change to visible menus or shortcuts).
- **Action Monitor**: copying an action row to the clipboard shows **Action row copied to clipboard** as the toast title (**Notify** **`message`**) with a caption line **Activity copied -** and the recorded action id (**de** / **fr** use the same pattern with localized strings); that positive toast skips the mirrored **`[Notify]`** row in developer tools (**copy failures** still omit the duplicate mirror next to **`console.error`**).
- **Developer tools**: every **Notify** toast also writes a matching line to the developer tools console (**console.error**, **console.warn**, **console.info**, or **console.log**) from toast **type** or **color**, tagged with **`[Notify]`** plus **message**, **caption**, **type**, **color**, and **timeout** when present, so you can correlate pop-ups with log output; paths that already emit their own **console** line for the same event skip the duplicate mirror.
- **Developer tools**: **Help & Info → Toggle developer tools** and the preload **openDevTools** path ask the bundled Chromium DevTools frontend for the **Console** panel after open when that internal API exists, rather than stopping on the default **Elements** tab.
- **Action manager**: failure **Notify** rows opt out of the automatic toast-to-console mirror when the handler already emits **console.error**, so error lines stay single (the same hook applies where **Action Monitor** row copy fails after its own **console.error** row).
- **Quasar palette** (**`app.palette.scss`**): **negative**, **positive**, and **warning** brand slots use deeper, less glaring hex values; **info** uses a fixed warm **#ffebb8** so framework-tinted **Notify** backgrounds stay coherent with dark-on-light treatment. Taken together, those brand slots align **Material**-mapped chrome with the Fantasia identity.
- **Load recent project**: the **Project Management** row for **Load Recent Project** matches **Advanced Project Tools** (grey side-arrow styling) and stays disabled when there is no recent list, instead of opening an empty submenu; each recent entry shows the project name with the **`.faproject`** path on a second line in the same muted style as menu shortcut hints, with no trailing icon.
- **Load project**: each **`.faproject`** now stores a persistent **project** UUID in SQLite; trying to load the same project again while it is already active shows a single clear message instead of reopening the file, and the **Notify** uses a **warning** style (not red) while the console and **Action Monitor** still record a failed action.
- **Notify** — **warning**-type toasts paint dismiss and other action-row **QBtn** chrome in the same dark tone as the message text so icons stay readable on the light **warning** background (call sites often still pass Quasar color **white**, meant for red error bars).
- **Desktop session** — a full main-window **WebContents** reload (for example **Control+F5** or the language spellcheck refresh) drops the main-process active **`.faproject`** database handle so the app matches a fresh renderer with **no** project open until you load one again.
- **Language control**: the spellcheck refresh icon beside the language selector uses the **`fa-icon-spellcheck-refresh`** class again so **`--fa-color-icon-spellcheck-refresh`** paints the red accent; it had referenced a non-existent **`fa-color-…`** utility name and fell back to the default light icon tone.
- **New project** — success **Notify** appears only once (**`faActionManager`**); the new-project dialog no longer posts a second toast with overlapping copy.
- **Tests** — end-to-end **New project** coverage targets **Project Management → New project** without mis-clicking welcome actions whose labels also contain **project** (**Playwright** uses an exact accessible name for the **Project Management** menu button).
- **Tests** — Vitest **unit-components** gained broader branch coverage for dialogs, floating windows, top-bar menus, **MainLayout**, language selector edges, and **Custom App CSS** injection via extra mounts plus small **`scripts/*.ts`** predicates tested directly where templates or timing made individual branches awkward to hit.
- **Tests** — end-to-end **Playwright** specs **`checkAppNoteboard`** and **`checkAppFloatingWindows`** cover **noteboard** scratch text persistence across restarts and floating **Custom App CSS** plus **App Noteboard** geometry persistence, **z-index** stacking, and coarse window reset flows; a **`WindowAppNoteboard`** component harness spec covers floating chrome and bridge text persistence in isolation.
- **New project** — the **New project name** field is focused when the dialog opens so you can start typing without an extra click.
- **New project** dialog: not persistent anymore, so backdrop and Escape close it again like other dialogs. Labels use **Create new project** and **New project name** (**en-US**, **de**, **fr**); the old muted **nameHint** line under the field is gone. The name field clears whenever the sheet opens; the card keeps tighter action spacing and the field uses centered width and **primary-bright** on the themed input shell. The modal component registry name is **`DialogNewProject`** (replacing **`DialogNewProjectSettings`**); **`faActionManager`** uses **`openNewProjectDialog`**, and **vue-i18n** paths use **`dialogs.newProject`**. **en-US** accessibility label lines up with **New project**.
- **New project** — the **OS save** dialog’s suggested **`.faproject`** file name keeps your spacing and capitalization when that stays safe across common filesystem rules instead of rewriting the stem to hyphenated lowercase; characters that paths reject are replaced by spaces or dropped for compatibility (**emoji**, control characters). The suggested default still includes the **`.faproject`** suffix together with the type filter so the offered path stays explicit across platforms.
- **New project** — **`.faproject`** paths are accepted when they are absolute on the host OS or use a **Windows** drive or **UNC** root, so validation matches real save dialogs even when main runs on non-Windows (for example automated testing).
- **New project** — the name field accepts at most **120** characters (**Quasar** **q-input** **maxlength** with a live character counter). The same cap applies to main-process validation, the suggested **OS save** stem, and names stored for **Load recent project**; if persisted recent-project data fails that rule (for example any name longer than **120** characters), the desktop shell clears the stored list until you open projects successfully again.
- **Documents & Content** submenu lists **Quick-Add New Document**, **Quick-Search Existing Document**, **Mass Delete Documents**, then **Export Project / Documents** in **en-US**, with aligned **de** / **fr** copies; **Export Project / Documents** moved here from **Project Management** earlier; **Import / Export App Configuration** lives at the bottom of **Settings & Tools**. Top menu **`title`** labels stay non-wrapping so long phrases remain on one row.
- **Custom App CSS** (**en-US**): the help panel heading for the theme variable list is **Fantasia Archive variable list** again (replacing the shortened **FA variable list** label).
- **Semantic text**: **danger emphasis** (**`fa-text-danger-emphasis`**, palette **`$text-danger-emphasis`**) uses a brighter red (**#ff4c4c**) for clearer contrast on dark chrome.
- **App Settings**: the selected **category** tab on the left uses the bright **primary** label color again. **fa-text-muted** now applies only to tabs that are not selected, so it no longer fights **Quasar** **active-color** when both utilities set **color** with **`!important`**.
- **Storybook**: Autodocs no longer shows the first story twice when a file defines multiple stories; **DialogActionMonitor** includes Autodocs like other dialogs; **Foundation** color palette tooltips omit the extra **SCSS** line for **`$info`**; **SocialContactButtons** drops the redundant **CompositionContract** story (orphan visual baseline removed).
- **Storybook** visual snapshots: the iframe readiness wait inspects **`#storybook-root`** and **`#root`** separately for element children (alongside visible portaled **Quasar** dialog or menu markup) so **`querySelector`** on a grouped **`#storybook-root, #root`** selector alone cannot mis-report an empty **`#root`** when the preview tree lives under **`#storybook-root`**.
- **Help → Changelog**: if a release line tried to show **CSS** rule text using **curly brace** block delimiters, **vue-i18n** could treat those characters as message placeholders; the dialog body might not render. The affected **2.4.12** line is rephrased in plain language.
- **QTooltip**: popups on dark chrome use **`$accent`** (**#f5f5f5**) and **`$dark`** (**#1b333e**), **13px** copy at weight **600**, **600px** max width, a high **`z-index`** for title-bar stacking, plus **`line-height` / `letter-spacing`** in **`quasarComponentsAdjustments`**, instead of **Quasar** stock tooltip sizing and colors.
- **Did you know?** (**info** **Notify**): the dismiss action is **`color: dark`**; **`_qNotify`** uses **dark** foreground on **`.q-notification.bg-info`**, and **`quasarComponentsAdjustments`** applies that **`color`** with **`!important`** so it wins over the same node’s **Quasar** **`text-white`** class and the global **`.text-white`** utility that also uses **!important**, which would otherwise force near-white on a light **info** background.
- **Welcome screen** and **About** social rows: the **Reddit** chip uses the wordmark image only (no separate text label; **en-US**, **de**, **fr**). Welcome copy uses heading markup and muted subtitle opacity. **SocialContactButtons** nests **row justify-center** wrappers so Patreon/Ko‑fi and secondary link groups stay horizontally centered on wide layouts.
- **About Fantasia Archive**: the dialog title uses a level-four heading so the document outline matches how the title reads visually.
- **Welcome screen** — primary actions use wider horizontal padding and **fit-content** width so each **button** sizes to its label instead of stretching the full actions column. **SplashPage** and **SplashControls** read layout from colocated **`styles/_variables.scss`** modules **`@forward`ed** through **`quasar.variables.scss`**, matching semantic **SCSS** token rules used elsewhere under **`src/components/`**.
- **Welcome screen** title row and the top-bar **language** dropdown suppress selectable text during drags so chrome labels do not pick up accidental highlight.
- **Dependencies**: **dompurify** (pulled in by **Monaco**) is pinned to **3.4.2** with a **Yarn** **resolutions** entry so the editor stack tracks a patched **3.x** sanitization release instead of an older transitive pin.
- **Electron desktop**: the **`BrowserWindow` `preload`** path resolves to Quasar’s packaged **`preload/electron-preload.cjs`** artifact so **`contextBridge`** APIs (**`window.faContentBridgeAPIs`**) initialize reliably instead of silently skipping preload when filenames or folders drift from the **`dev`** bundle layout.
- **Action Monitor**: successful **Load project** history rows keep the enqueue payload empty but attach a closing preview with the chosen **`.faproject`** path and loaded project name, aligned with how **New project** surfaces detail after completion; when opening fails after you pick a file, failed rows can include the message plus the attempted path when the desktop shell supplies it.
- **Electron desktop**: **`faRoutingEnv`** waits briefly for the preload **extra environment** snapshot before steering initial harness routes, avoiding a startup race where the bridge attaches after first renderer paint.
- **Playwright helpers**: Harness code for Electron component and **E2E** suites is split under **`helpers/playwrightHelpers_universal`**, **`helpers/playwrightHelpers_e2e`**, and **`helpers/playwrightHelpers_component`**, with shared launch and teardown helpers so serial **`beforeAll`** blocks stay uniform; **`README`** and contributor notes list the updated import paths.

---

## 2.4.11 - Version bump

### New features
- **Top bar**: the **Tools** menu label is **Tools & settings** (and matching **de** / **fr** strings) so the name matches settings-related entries inside the menu.
- **Locale parity**: **de** and **fr** now follow the same **vue-i18n** key tree as **en-US** (including **errorNotFound**, **globalWindowButtons**, **fantasiaMascotImage**, **dialogs.markdownDocument**, **globalFunctionality.unsortedAppTexts**, and bundled **documents**), so window chrome, the error page, the tips heading, and mascot alt text resolve without wrong-cased or missing keys. **App settings** in **de** still uses the **en-US** string module until a full **de** app-settings pass lands; **fr** keeps its partial **app settings** file with **en-US** fallback for the rest. **Import / export** notice lines were tightened in **en-US** with matching **de** / **fr** updates.
- **Import / export app configuration** — the **Import** / **Export** / checklist panels swap with a horizontal slide (stepper animation); the step header stays hidden so the flow still looks like one dialog.
- **Import / export app configuration** — the top of the dialog includes a localised **Please note** block (export-first guidance, overwrite warning, and selective-import option) in **en-US**, **de**, and **fr** before the **Import** / **Export** chooser.
- **Tools → Import / Export app configuration** (**mdi-database-export-outline**), directly under **Show noteboard**: opens a non-persistent **Import / Export app configuration** dialog (outside clicks and **Escape** close it) with **Import** and **Export** on the first step in two columns with a vertical separator, each button full width of its half with **8 px** horizontal inset for the action row, flat **primary** actions (bold, uppercase labels), matching the classic **FA 1.0** **Insert image** dialog. **Export** offers three pre-checked checkboxes (**App settings**, **App keybinds**, **Custom app CSS**), a **Create export file** control (enabled only if at least one part is selected), and then the normal **OS save** dialog to write a **`.faconfig`** (ZIP of the same **`electron-store`** JSON names as the live app: **`faUserSettings.json`**, **`faKeybinds.json`**, **`faAppStyling.json`**). **Import** uses the open-file dialog for **`.faconfig`** only; the main process enforces a **3 MB** cap, allowlisted members, **JSON** + **Zod** validation, and staging before you confirm **Import selected**; applying replaces the chosen stores and the renderer reloads app settings, keybinds, and app **CSS** from the bridge. The flow records **faActionManager** actions (**openImportExportAppConfigDialog**, **exportAppConfigPackage**, **exportAppConfigSaveResult**, **importAppConfigStageResult**, **importAppConfigApply**) for the **Action monitor** with small, monitor-safe payloads (no file bodies).
- **Tools → Custom app CSS**: a new entry opens a **persistent** **Custom app CSS** floating window (icon **mdi-language-css3**) with a cold-loaded **Monaco** editor configured for **CSS** language support. The window is **movable** (drag the title row) and **resizable** (edge and corner handles); it can only be closed with **Close without saving** or **Save changes** (**Escape** and random clicks outside the window do not dismiss it). Saved CSS is stored in the **Electron** main process via **electron-store** and applied globally as a **`<style id="faUserCss">`** element injected into **`document.head`** at app load and refreshed whenever you save changes, so custom rules apply across every route, layout, and embedded surface. The frame is an in-renderer **`position: fixed`** surface teleported to **`document.body`**, centered and sized from viewport fractions and margins, with **Vue** **Transition** and the custom **fa-floatingWindowPop** animation (opacity and scale; minimum scale is never **0**; **300 ms** like a default **QDialog**; not stock **q-transition--scale** **scale(0)**, which can leave a degenerate **transform** in **Electron**). **Monaco** is loaded only when the window opens (and only the **CSS** language pack and worker are bundled) so initial app startup is unaffected. **Tools → Keybind settings** lists **Open custom app CSS** with a default shortcut of **Ctrl + Alt + Shift + J** (**Cmd** replaces **Ctrl** on **macOS**); the chord can be reassigned or cleared from there. Both opening and saving flow through the **action manager** (**openAppStylingWindow** and **saveAppStyling**), so save failures surface the same single-notification treatment as other settings surfaces. (The upstream **[@quasar/quasar-ui-qwindow](https://github.com/quasarframework/quasar-ui-qwindow)** package targets **Quasar v1 / Vue 2** only; Fantasia Archive ships an in-app **Vue 3** floating frame instead.)
- **Action manager**: every user-meaningful action (open dialog, save settings, minimize / resize / close window, language switch, dev tools toggle, startup tips notification, and similar) now flows through a single dispatcher with two execution kinds — **async** (fire-and-forget) and **sync** (single FIFO queue, soft cap **32**, **30 s** per-action timeout, opt-in deduplication). Failures show a single **error** notification with the action id and the underlying error message, and the same details land in the developer console row alongside it. Save flows for **Keybind settings** and **App settings** route through this surface, so the previous double notifications on a failed save are gone.
- **Help & info → Action monitor**: the entry under **Toggle developer tools** opens an **Action monitor** dialog with a table of actions from the current app session: **Action** id, **Start time**, **Finish time**, **Payload** (green check when enqueue-time payload text exists, grey dash when empty), **Type** (**Sync** or **Async**), and **Status** (gear spinner while running, green check on success, red cross on failure, blue sand-timer icon while queued, each with a hover tooltip for the meaning). A help icon explains that a left click copies row details to the clipboard. The grid is a **snapshot** when the dialog opens, capped at **500** rows per session, not written to disk. **Keybind settings** lists **openActionMonitor** so you can assign a global shortcut; there is still no default chord.
- **Tools → Keybind settings** opens a **Keybind settings** dialog (table of actions, default and custom shortcuts, filter, save to persistent storage in the **Electron** main process). **Help & info** no longer lists a separate keybind cheatsheet entry; shortcuts are managed from **Tools**. Global shortcuts stay active while typing in inputs where the binding allows it, and dispatch pauses while this dialog or its capture popup is open.
- **Interface language**: a **language** control in the top bar (next to the window buttons) shows the current locale as a **flag** in a circular control and opens a short menu for **English, US**, **Deutsch**, and **Français**. The choice is stored as **languageCode** in persistent **user settings** (**en-US**, **de**, or **fr**, default **en-US**) and applies **vue-i18n** across the app. **App settings** does not expose this field yet.
- **Country flags**: bundled a full set of **SVG** flag images under **`public/countryFlags/`** for the language control and other locale-aware UI.
- **German** **top bar**: **Project**, **Documents**, **Tools**, and **Help & info** menu entries use **Deutsch** locale modules alongside **About** and the shared **social** row, matching the **en-US** menu shape.
- **Advanced Search Guide**: default shortcut **Ctrl + Alt + Shift + G** opens the markdown guide in the same dialog as **Help & Info → Advanced Search Guide**; **Tools → Keybind settings** lists it as **Open Advanced Search Guide**.
- **Menu shortcut hints**: **Tools** and **Help & info** show the live global shortcut next to **App settings**, **Keybind settings**, **Advanced Search Guide**, and **Toggle developer tools**, using the same labels as **Tools → Keybind settings**; the hint line appears only after keybinds load from **Electron** and only for those actions.
- **Storybook**: **AppControlSingleMenu** and **AppControlMenus** stories include **`keybindCommandId`** fixtures (top-level and submenu), a composition check that opens **Help & info** and expects shortcut hint markup, and an optional **`embedDialogs`** variant so dialog hosts mount like production.
- **End-to-end**: the **`checkKeybinds`** Playwright spec covers default **Ctrl + Alt + Shift + G** and a persisted custom chord for **Open Advanced Search Guide** against the markdown guide dialog, in the same flow as other global shortcuts.
- **End-to-end**: the **`checkAppStyling`** Playwright spec opens **Custom app CSS** from **Tools**, enters **CSS** in **Monaco**, saves so **document.body** picks up **zoom** from the injected stylesheet, reopens the styling window, clears the editor, saves empty **CSS**, and asserts **zoom** returns to the default.
- **Storybook**: **`externalFileLoader`** registers **`floatingWindows.appStyling`** for **en-US** so **WindowAppStyling** **autodocs** and canvas stories resolve titles, tooltip copy, and action labels instead of raw **vue-i18n** keys; the story seeds multi-line sample **CSS** so **Monaco** syntax highlighting is easy to spot.
- **Storybook**: **DialogImportExportAppConfig** canvas and **autodocs**; **`externalFileLoader`** registers **dialogs.importExportAppConfig**; the workspace content bridge mock adds **faAppConfig** stubs so **prepareImport** can succeed in the canvas while **exportToFile** returns **canceled**, matching the real flow enough to review the dialog without **Electron**.
- **End-to-end**: **`checkImportExportAppConfigFlow`** runs the import / export app configuration flow (export, staged import, **Action monitor** checks, and related **Keybind** / app styling side effects) using fixed paths in the isolated **Playwright** profile; **E2E** can skip native file dialogs for those steps via a main-process seam that applies only when **`TEST_ENV`** is **e2e**. **Playwright** shortcut strings use **`helpers/playwrightHelpers_universal/faPlaywrightKeyboardChords.ts`** so defaults that follow the app **primary** modifier and chords that use the physical **Control** key match **macOS** and **Windows** / **Linux** hosts.
- **Component test**: **`DialogImportExportAppConfig.playwright.test`** covers close-on-**Escape** for the import / export dialog in the **component** harness.

### Bugfixes & Optimizations
- **Playwright** (component, **E2E**, and **Storybook** visual) uses the terminal **`list`** reporter: each test prints on its own line while the run progresses (HTML reports unchanged).
- **Top app menus** (**AppControlSingleMenu**): after a full-width **separator** row in the menu data, the thin divider before the next item no longer stacks on the full separator (no double horizontal rules).
- **Import / export app configuration** — the **OS** save and open dialogs default to your **Downloads** folder, and export suggests the filename **faConfigExport.faconfig** so the offered path is consistent when a bare filename would otherwise land in an unexpected folder on some systems.
- **Custom app CSS** floating window — the frame **Transition** uses a custom **fa-floatingWindowPop** animation (about **0.1** to **1** scale plus opacity, same **300 ms** as a default **QDialog** and standard easing) instead of stock **q-transition--scale**, which can leave the root at a degenerate **transform** in **Electron** and break **Monaco** in **Playwright** visibility checks.
- **Custom app CSS** floating window — resize edge and corner handles show a soft **primary** gradient toward the outer edge only while you hover or drag; inactive handles stay invisible. The glow fades in and out with a short opacity transition. Handle colors are theme tokens in **quasar** variables; rules live in **`_FaFloatingWindowFrameResizeHandles`** **`styles`** so the **Vue** file stays within line limits.
- **Floating windows**: pointer resize updates frame width and height at most once per display frame, which cuts redundant layout work for **Monaco** and the rest of the surface while you drag a handle.
- **Custom app CSS** floating window — the title text no longer forces **`margin: 0`**, so the heading keeps normal spacing next to the drag row and help icon.
- **Custom app CSS**: while the floating editor is open, **Monaco** changes apply live to the global **`#faUserCss`** stylesheet so you can preview rules before saving. **Close without saving** reloads **CSS** from disk (same bridge read as at startup) so the app returns to the last persisted file.
- **Custom app CSS** floating window — **Layout** no longer caps width at **1400 px**. **CSS** uses **`max-width: stretch`** instead of **`100vw`** so the frame stays inside the visible layout when scrollbars or insets apply. **`<Transition>`** wraps the frame root, not **Teleport** (so enter/leave run on a single element and **Vue** does not warn about non-element transition children); open/close use **fa-floatingWindowPop** (see **Bugfixes**). **_FaFloatingWindowBodyTeleport** sends the frame to **`document.body`** so **Storybook** and tests do not depend on a **`#q-app`** mount target. The title row uses a flex **min-height** so the drag strip keeps a real hit target. Floating surfaces use a **z-index** band below main chrome. After close, **Monaco** disposes only after the dialog transition; a pending dispose is cancelled if you reopen or close again before that delay finishes.
- **Playwright** **yarn** scripts: **`scripts/playwrightWithArtifactTrim.mjs`** turns **`--component=bucket/ComponentFolder`** into **`src/components/...`** when **Yarn** on **Windows** does not expand **npm** config placeholders, and removes **test-results/playwright-artifacts** after a run so duplicate attachment trees are not left on disk.
- **Custom app CSS** **directInput** wiring (harness and **Storybook**): the **`directInput`** prop watcher runs **immediately** on setup so **`WindowAppStyling`** opens when **`directInput`** is already **WindowAppStyling** when args hydrate.
- **End-to-end** **`checkAppStyling`**: readiness and close checks use the **`windowAppStyling-frame`** locator instead of relying on title visibility alone after the window opens.
- **Electron renderer protocol**: packaged builds now load the renderer through a custom privileged **`app://`** scheme registered in the main process (**standard**, **secure**, **supportFetchAPI**, **stream**, **codeCache**, **bypassCSP**) instead of **`file://`**. The handler resolves the renderer root next to the running main bundle, refuses path traversal and **`..`** segments, and falls back through **`index.html`**. This is what lets the **Custom app CSS** window spin up its **Monaco** worker (**css.worker** loaded with **Vite** **`?worker`** imports) reliably in the packaged app, since web workers do not initialize the same way under **`file://`**. The dev server flow is unchanged.
- **Global keybinds**: built-in defaults on **Windows** and **Linux** are now **Ctrl + F12** for **Toggle developer tools**, **Ctrl + Alt + Shift + L** for **Open app settings**, **Ctrl + Alt + Shift + K** for **Open keybind settings**, **Ctrl + Alt + Shift + J** for **Open custom app CSS**, and **Ctrl + Alt + Shift + G** for **Open Advanced Search Guide** (on **macOS**, **Command** replaces **Ctrl** where the chord uses the primary modifier; **Open Advanced Search Guide** always uses **Ctrl**). Existing saved overrides in the **Electron** main store are unchanged.
- **Markdown document** dialogs opened with **directInput** (for example **license** or **changeLog** from menus) now register the same **markdown dialog stack guard** as routed opens, so **App settings** and other component dialogs cannot stack on top until you close the markdown sheet.
- **External links** boot wires a small **`faExternalLinkMouseDispatch`** helper so the click and auxclick logic stays testable in **Node** Vitest (injected **`getBrowserWindow`**) while production still reads **`globalThis.window`**. The handler no-op path runs when **`faExternalLinksManager`** or its methods are missing so non-**Electron** renders do not throw.
- **Global keybinds**: if loading shortcuts from the **Electron** main process fails, the app shows a **Quasar** notification with a short **load error** message instead of only logging; the same message appears if **Tools → Keybind settings** cannot refresh on open.
- **Component test harness**: invalid **COMPONENT_PROPS** JSON from **`extraEnvVariables`** is ignored (**false**) instead of crashing the main-process **IPC** handler.
- **Storybook** visual regression: **`yarn test:storybook:visual`** now fails when a story’s iframe never shows rendered content under the Storybook root (and no visible portaled dialog or menu), unless that story opts out with **`skip-visual-render-check`** or its **id** is listed in **`EXCLUDED_STORY_IDS`** in the visual Playwright spec.
- **Contributor docs**: **README**, **AGENTS.md**, **CONTRIBUTING**, **testing-terminal-isolation**, **storybook-stories**, **git-conventional-commits**, **fantasia-dev-setup**, **fantasia-testing**, and **fantasia-changelog-en-us** stay aligned on **Yarn** **1.x** and CI **1.22.19**, **i18n** **components** buckets including **dialogs**, **testbatch:verify** coverage tiers (including **i18n**), repo-root **helpers** only for harness code, the same VRT rules, correct **Husky** **`yarn lint:commit --edit`** wording, and repo-root-relative links in the changelog skill.
- **Contributor docs**: **README**, **AGENTS.md**, **playwright-tests**, **testing-terminal-isolation**, **fantasia-testing**, and **fantasia-keybinds** document cross-OS **`keyboard.press`** rules for app keybinds and point to **`helpers/playwrightHelpers_universal/faPlaywrightKeyboardChords.ts`**.
- **Contributor workflow**: **AGENTS.md** and Cursor **rules** / **skills** (**changelog-en-us**, **git-conventional-commits**, **testing-terminal-isolation**, **fantasia-changelog-en-us**, **fantasia-i18n**, **fantasia-testing**, **storybook-stories**) describe folding **`changeLog.md`** into the same commit batch as substantive work before **push**, using changelog-only commits only for rare fixes, and keeping Git or housekeeping phrases out of in-app release notes.
- **Keybind settings** capture treats physical modifier keys (**ShiftLeft**, **ControlRight**, **AltLeft**, and similar) as incomplete input instead of pairing them with duplicate modifier labels, and accepts shortcuts built from letters, digits, **F1–F24**, the four arrow keys, and a fixed set of typing punctuation keys (comma, brackets, slash, and similar) — not numpad-only keys or most other special keys. Those guidance lines use the field **hint** (informational styling); **duplicate shortcut** on **Set** still shows as a field error.
- **Keybind settings** capture popup aligns with **legacy Fantasia Archive (FA 1)** capture polish: the **action** name as the title, a **Type your keybind** placeholder, a **thin dotted** underline under the chord line (**red** only for duplicate conflicts, muted tint when **modifier** or **unsupported-key** hints apply), and **one compact panel directly under** the chord row whose **text swaps** between the **need modifier** / **unsupported key** guidance and the **duplicate shortcut** message depending on what just happened (**error** tone and **alert** role only for the duplicate case). The panel **fades and slides** in that slot instead of stacking two separate message areas. The conflict **alert** icon stays beside the chord row when a duplicate is active; the old standalone **info** icon for hints is gone. The **help** tooltip still lists modifier patterns and accepted keys; **Clear keybind** and **Set** stay on the light panel. Physical modifier-only presses still avoid painting the placeholder red; a lone modifier press after **need modifier** or unsupported-key guidance can clear that hint again.
- **Keybind settings** capture **Clear keybind** sets that action’s working override to **null** so the main table’s **User keybinds** column immediately matches the built-in default again (**Add new…** when that default is what is active, same rule as persisted **null** overrides), closes the capture sheet, and stays disabled while the chord line only shows the empty placeholder (no shortcut text yet).
- **Keybind settings** capture flags a duplicate shortcut as soon as the chord is recognized, using each row’s **active** binding (defaults plus unsaved overrides). A default that no longer applies because another action was rebound does not block reuse. The duplicate message is clearer in **English**, **Deutsch**, and **Français**; a stray modifier key after a conflict no longer clears the warning alone. While that duplicate error is shown, the capture line stays on the shortcut that was visible when you opened capture (or stays empty when that row had no shortcut) instead of showing the rejected chord; **Set** with a conflicting chord does the same.
- **Keybind settings** capture opens with the row’s **current** shortcut already shown (defaults and custom overrides), drops the redundant **Use default** control from the capture sheet, and nests the capture popup inside the main keybind dialog. The main **Keybind settings** sheet stays **persistent**; you can close the capture sheet with an **outside click** or **Escape** without closing the parent dialog. **Escape** on the capture sheet is handled in the capture listener so it always dismisses that sheet instead of being treated as an unsupported shortcut key.
- **Keybind settings** dialog uses a **1200 px** card width on wide viewports (it still shrinks on narrow windows), table headers **User keybinds** and **Default keybinds**, and the filter placeholder **Filter keybinds…**. The **User keybinds** **Add new…** control uses **primary-bright** outline while the active shortcut matches the built-in default or nothing is assigned yet, and **grey-7** outline with the formatted chord only when a custom shortcut differs from that default.
- **Keybind settings** **Default keybinds** column shows an **em dash** when the built-in default chord cannot be expanded for the current platform, instead of misleading formatted text.
- **Keybind settings**: the shortcut table uses the vertical space between the dialog title and the footer action row, and its scroll height updates when you resize the window.
- **Keybind settings**: the shortcut table lists only real shortcut actions from the keybind list; development-only filler preview rows are removed.
- **Keybind settings** capture help tooltip (**English**): clearer summary of allowed keys and **your key** placeholder wording in modifier combination examples.
- **Keybind settings** filter with no matches shows the same **ErrorCard** treatment as **App settings** search (mascot in **reading** mode, title and supporting text from **vue-i18n**) via the **q-table** **no-data** slot instead of a bare empty table. The table bottom bar is hidden only while rows exist, because **Quasar** paints **no-data** inside that bar and **hide-bottom** skips it entirely when always on. **Virtual scrolling** on that table turns off while the filtered row list is empty so **Quasar** still mounts the **no-data** slot instead of leaving a blank virtualized body. Clearing the filter field no longer throws when **Quasar** sets **v-model** to **null** instead of an empty string. The filter field resets to empty whenever the dialog hides, including after **Save settings** or **Close without saving**.
- **Electron spellcheck**: Chromium’s spellchecker language tracks the same persisted **languageCode** as the interface (**en-US**, **fr**, or **de**), and the **Add to dictionary** context menu label matches that choice instead of the operating system locale. **Français** and **Deutsch** no longer fall back to **English** when Chromium’s available list omits the exact hunspell keys we first look for; we match **fr-*** / **de-*** tags in list order, then try the short **fr** / **de** codes when needed. After a language change, the main **session** turns spellcheck off, clears the previous dictionary list, sets the resolved language, and turns spellcheck back on. The title-bar **language** control can show an optional **reload** action after you pick a different interface language so you can refresh the window yourself if spellcheck in an already-open field still follows the previous dictionary (Chromium sometimes keeps the old binding until a navigation).
- **Top bar language** control: **Deutsch** and **Français** strings for the optional spellcheck **reload** hint (**aria-label** and multiline tooltip) mirror the **English** wording and line breaks.
- **Language bar flags** and **social** toolbar icons: **public/** assets (for example **countryFlags** SVGs and **images/socialContactButtons**) now resolve with the same **Vite** **BASE_URL** rules as the rest of the renderer, using a **relative** base when the app runs from **file://** in packaged **Electron**, so flags and icons load instead of breaking on root-absolute **/** URLs.
- **About Fantasia Archive** dialog: **German** copy for the title, version line, **Close**, and the social link row (**Patreon**, **Ko-Fi**, **Website**, **GitHub**, **Discord**, **Reddit** tooltip, **X**). **French** already had dialog and button strings; the locale tree now also registers **`socialContactButtons`** under the same camelCase key as **English** so those labels resolve in **Français** instead of falling back.
- **Language menu**: after picking a new interface language, the **settings saved** toast uses the **new** locale instead of the previous one.
- **Language menu**: the **en-US** option label is **English, US** (was **English**) in every interface locale.
- **Top app menus** (**Project**, **Documents**, **Tools**, **Help & info**): labels now follow the active **vue-i18n** locale when you change language (they were resolved once at startup and stayed in the initial language). **AppControlSingleMenu** also re-reads **title** and **items** when the parent passes updated menu data.
- **Window controls**: the custom title bar **maximize** / **restore** toggle saves the window bounds before **maximize** and reapplies them after **unmaximize** so restoring from a maximized state returns to the previous size on **Windows** with a frameless **Electron** window, where **unmaximize** alone could leave the window at the maximized dimensions.
- **Electron** **preload** bridge: **Window** chrome, **DevTools**, the packaged **version** string, and the harness **extraEnvVariables** snapshot use **async** **IPC** via **`ipcMain.handle`** and **`ipcRenderer.invoke`** instead of blocking synchronous channels, so the **renderer** does not stall on those calls.
- **App startup and component tests**: **`faRoutingEnv`** boot loads the harness snapshot, then **awaits** startup routing so **`router.push`** finishes before the shell renders. **`ComponentTesting`** resolves the tested **SFC** from the route reactively and seeds **COMPONENT_PROPS** from **`extraEnvVariables.getCachedSnapshot`** when boot has already resolved **`getSnapshot`**; **`AppControlMenus`** reads **`TEST_ENV`** the same way for the first paint. **Preload** **`extraEnvVariables`** adds **`getCachedSnapshot`** after the first successful **`getSnapshot`** resolve. **ComponentTestingLayout** now loads persisted **user settings** from main on mount in **Electron** and reapplies **vue-i18n** from **languageCode**, matching **MainLayout** so a full window reload during isolated component runs keeps the chosen interface language.
- **Global window buttons**: accessible names live on **`aria-label`** only; **Quasar** tooltips and the **`title`** attribute were removed from those title-bar buttons.
- **App settings** search with no matches: empty-state **ErrorCard** shows heading **No search match** and longer guidance from **vue-i18n** as **details** below the mascot (**en-US** and **fr**). **ErrorCard** exposes optional **details** only there, with **data-test-locator** hooks **errorCard-title** and **errorCard-details**; **Error not found** keeps title and multi-line **details** only.
- **Storybook** **`externalFileLoader`** mock registers **dialogs.markdownDocument** from **L_markdownDocument** so **DialogMarkdownDocument** stories resolve the **Close** label and related strings instead of raw keys.
- **Storybook** **`externalFileLoader`** mock registers **dialogs.actionMonitor** so **DialogActionMonitor** stories resolve titles, column headers, and button labels instead of raw translation keys.
- **Storybook** visual capture calls **page.waitForFunction** with **undefined** as the injected-argument parameter before the options object so Playwright applies the render timeout instead of waiting without an inner deadline.
- **Storybook** — contributor **.storybook-workspace** config disables the sidebar onboarding checklist, turns off **What's New** notifications in **core** options, and **manager-head** hides the green unread dot on the settings gear for **yarn storybook:run** and static builds.
- **App settings** Playwright expectation helpers import **dialogAppSettingsTreeOrdering** instead of **dialogAppSettingsTree** so **Node** does not load **app/i18n** markdown raw imports when resolving the spec module graph.
- **FantasiaMascotImage** centers art horizontally when the bitmap is narrower than its slot (**margin: auto** on the inner **img**).
- **External links**: **http** and **https** URLs are validated with a real URL parse and an **http** / **https** scheme allowlist before **openExternal** runs in main or the preload **checkIfExternal** helper runs in the renderer. Loopback and non-routable targets are blocked using the parsed host (**localhost**, hostnames ending in **.localhost**, **127.0.0.0/8**, **::1**, IPv4-mapped loopback, **0.0.0.0**), percent-encoded **localhost** is still recognized, and scheme tricks such as **nothttps://** no longer pass as **https**. Hostnames that merely contain the letters of **localhost** inside a label (for example **notlocalhost.example**) stay allowed when the parsed host is not loopback.
- **User settings**: the **Electron** main process validates each save patch with **Zod** before merging into **electron-store**, accepting only known keys (**boolean** toggles and **languageCode** as **en-US** / **de** / **fr**) and rejecting unknown or wrongly typed fields. The **S_FaUserSettings** Pinia store shows the existing save-error notification when **setSettings** fails (including validation or bridge errors) instead of only logging an unhandled rejection.
- Shipped version is now **2.4.11** in **About** and packaging metadata.
- **Contributor docs**: **README**, **AGENTS.md**, **electron-preload** rule, and **fantasia-electron-main** / **fantasia-electron-preload** skills describe **renderer sandbox** (**BrowserWindow** **webPreferences**), official **Electron** sandbox and context-isolation links, privileged **main** **IPC** (harness **extraEnvVariables**, external **http** / **https** links), **startApp** registering **ipcMain** handlers before the first window, an **async-first** preference for **invoke** / **handle** over synchronous **IPC** when any reasonable async design exists, when to validate structured **IPC** payloads with **Zod** in main versus **typeof** plus domain checks, and which handlers stay scalar-only. Short comments on **electron-preload** and **electron-main** echo the same guardrails.
- **Contributor workflow**: colocated **Vitest** files, **Playwright** component specs, and **Storybook** stories now sit under **`_tests/`** directories across **`src`**, **`src-electron`**, **`helpers`**, **`i18n`**, and **`scripts/_tests`** next to extracted script modules where used (the former folder segment name was **`tests`**). **Vitest** coverage excludes, **Storybook** story discovery globs, and contributor guidance (**README**, **AGENTS.md**, Cursor rules and skills) follow the new path. Top-level **`e2e-tests`** and **`.storybook-workspace/visual-tests`** are unchanged.
- **App settings**: while the header search field has text, the dimming scrim again covers the full **dialog card**; the title, scrollable body, search field, and footer actions stay usable where their stacking order keeps them above the overlay.
- **App settings** implementation is split into smaller **Vue** subcomponents and colocated **scripts** modules so the dialog shell stays within the enforced line limits without changing the visible layout.
- **Contributor maintenance**: shared **TypeScript** contracts for bridges, dialogs, keybind data, user settings, and related helpers are grouped in repository-root **`types/`** with brief **JSDoc**, replacing many single-purpose type files and colocated **`*.types.ts`** next to **`src/`**, **`src-electron/`**, and **`Storybook`** mocks. **Keybind settings** and **App settings** colocated **`scripts/`** code is merged into fewer modules apiece for the same runtime behavior and easier navigation while respecting **ESLint** line budgets. **README**, **AGENTS.md**, Cursor rules, and skills describe preferring **fewer logically grouped** **`scripts/`** and **`src/scripts/`** modules until line limits force a split, and consolidating colocated **`scripts/_tests/`** when production scripts merge. Empty legacy **`tests/`** directories left over from the **`_tests/`** rename were removed from the tree.
- **Contributor tooling**: **ESLint** enforces line-count limits on production **Vue**, **TypeScript**, and **JavaScript** (tests, **Storybook**, configs, **i18n**, type-only modules, and similar paths stay exempt). **README**, **AGENTS.md**, and Cursor rules describe the caps—**Vue** SFC **250** lines, **TypeScript** module **200** lines, function **100** lines—and when to split into **`scripts/`** or subcomponents. Returned object literals in production code must list only **prior bindings** for property values (no inline functions or ternary logic in the literal); see **`.cursor/rules/code-size-decomposition.mdc`**. Moving scoped **CSS** to a standalone file and re-importing it stays an exception that needs explicit maintainer approval.
- **Contributor maintenance**: production **`return`** surfaces that use object literals (including **action manager** queue payload shaping, keybind chord helpers, **AppControlMenus** item builders, **DialogActionMonitor** / **Keybind settings** / **App settings** table helpers, **Electron** **IPC** snapshot builders, and **Playwright** **`recordVideo`** options) now bind **`const`** values first so returned object properties stay simple identifiers or literals. **Action manager** implementations live in **`src/scripts/actionManager/faActionDefinitionHandlers.ts`**, with **`faActionDefinitions.ts`** holding the **registry table** only; **AGENTS.md** and the **fa-action-manager** Cursor rule/skill describe the split.
- **GitHub Actions** manual **Build App** workflow (**.github/workflows/build.yml**): the **Windows** job runs **yarn testbatch:ensure:nochange** end to end (same quality gate as labeled **PR** full-suite runs, plus one production **Quasar** **Electron** build reused for **Playwright** and **Storybook** checks), then uploads the **Windows** installer without a second packaging build. **Linux** jobs start only after that gate succeeds and run as a matrix so each leg uploads its own artifact set: **AppImage**, **Debian** package, and **RPM**. **macOS** still runs one production build and uploads the **.dmg**. **Storybook** visual baselines stay **win32**-aligned, so the full gate remains on **windows-latest**.
- **Contributor docs**: **README**, **AGENTS.md**, **vue-quasar** rule, and **fantasia-quasar-vue** skill document one **Vue** single-file layout for every **.vue** file: **template** first, then **script**, then **style** (including **Storybook** workspace SFCs). **App settings** helper components were reordered in source to match; visible behavior is unchanged.
- **Contributor maintenance**: the cross-feature **Vue** helpers for floating-window body **Teleport**, floating-window resize handles, and the global **`#faUserCss`** injector use **`_<PascalCase>`** folder and **`.vue`** names (**`_FaFloatingWindowBodyTeleport`**, **`_FaFloatingWindowFrameResizeHandles`**, **`_FaUserCssInjector`**) so they read as infrastructure next to **`Window*`** and **`Global*`** surfaces; **AGENTS.md** limits that prefix to these three. Earlier notes in this log that name those pieces now use the same spelling. App behavior is unchanged.
- **Contributor maintenance**: renderer **TypeScript** helpers are regrouped under **`src/scripts/appInternals/`** (locale apply helpers, language typing, public asset paths, **Storybook** canvas detection) and **`src/scripts/appGlobalManagementUI/`** (markdown and component dialog stack wiring, component-test mascot routing, dev-tools toggle, startup routing, trivia notifications) with clearer exported names than earlier **lease**-style entry points. **Global keyboard** preprocessing and dispatch live in **`src/scripts/keybinds/faKeybinds*`** modules that **`MainLayout`** composes through **`createFaKeybindKeydownHandler`**. The **preload** external-links helper module is now **`faExternalLinksManagementAPI`** (behavior unchanged; name aligns with other management APIs).
- **Storybook** workspace **Vite** adds **`server.fs.allow`** for the repository root so **Keybind settings** **`.vue`** files can resolve sibling **`types/`** contract imports during static builds and visual captures.
- **Storybook** static **visual regression** (**`yarn test:storybook:visual`**): **Vite** resolves **Pinia** and **Vue** from the repository root so the nested workspace does not load a second **Pinia** copy that prevented dialog stories from mounting in the **iframe**. The **Playwright** harness serves the static build on a dedicated port and does not reuse an existing server there, so a local **`yarn storybook:run`** session on the default port cannot satisfy captures by accident. Failed captures still attach a full-page screenshot and a short **JSON** probe of the **Storybook** root when navigation, render checks, fonts, overlays, or the screenshot step fails.
- **Contributor maintenance**: **Vitest** specs for **AppControlMenus**, **AppControlSingleMenu**, **GlobalLanguageSelector** (including the spellcheck refresh delay path), **GlobalWindowButtons**, **ErrorCard**, **App settings** setting blocks, and expanded **dialog** settings screens exercise top-bar harness branches, language picking, window chrome timers, and **Quasar** menu click paths in jsdom. Multi-stub dialog specs use the same file-level **ESLint** exception already applied to stack-guard harness tests.
- **Contributor maintenance**: more **Vitest** jsdom coverage for **DialogActionMonitor** dialog show refresh, **DialogImportExportAppConfig** opens and import staging, **DialogKeybindSettings** when the keybind definition list is empty, **_FaFloatingWindowFrameResizeHandles** hover and every resize pointer target, **WindowAppStyling** editor help tooltip content plus loading and load-error overlays and frame pointer wiring, **MainLayout** app styling refresh when the **Electron** bridge exposes it or not, **AppControlMenus** component-testing markdown menu trigger, **AppControlSingleMenu** submenu rows without click handlers, **GlobalLanguageSelector** when the user-settings bridge is absent, and **_FaUserCssInjector** skipping duplicate style text writes.

---

## 2.4.10 - App settings, ErrorCard, theme tokens, and contributor tooling

### New features
- **Tools → App settings** opens the **App settings** dialog (persistent until **Close**; **Save** is reserved for later wiring).
- **App settings** lists every persisted boolean as **category → subcategory → toggle** rows with **en-US** / **fr** copy (title, description, optional red **note**). **Category** and **subcategory** tabs follow key order except **Accessibility** (always second-to-last) and **Developer settings** (always last); **toggles** within a subcategory sort by translated **title** (ties use the setting key). **Show document IDs** is under **Developer settings → Document body**. A top-level **Accessibility** tab groups wider scrollbars, tree count dividers, strike-through hiding, and text shadow; theme and related items stay under **Visuals & App-wide functionality** without nesting that block there.
- Added persistent **user settings** in the **Electron** main process (**electron-store** under app **userData**), starting with a **theme** preference (**light** or **dark**, default **light**), exposed to the renderer as **`window.faContentBridgeAPIs.faUserSettings`** (**get** / **set**).
- Added **`S_FaUserSettings`** Pinia store for managing user settings state in the renderer: loads the full settings set once on app start via the IPC bridge, accepts patch-based updates, and shows a **Quasar** success or failure notification after each save attempt.
- **App settings** uses vertical **category** tabs (**`q-tabs`** / **`q-tab-panels`**) and a **scrollable** main column (**`q-scrollarea`**): the **title**, tab rail, and **Close** / **Save** stay fixed while content scrolls. Subcategory toggles use a responsive grid (**one** / **two** / **three** columns by breakpoint), place the **toggle** under the title, and show descriptions from a help icon **tooltip**.
- **App settings** reloads persisted toggle values from **`S_FaUserSettings`** when the dialog opens using a guarded **Pinia** resolve so environments without an active user-settings store skip hydration instead of throwing. Help-icon alignment next to multi-line titles is nudged for clearer vertical alignment with the first line.
- **ErrorCard** (**`src/components/elements/ErrorCard/`**): reusable **q-card** layout for error states (**title**, optional multi-line **details**, **FantasiaMascotImage** variant, configurable **max-width**); colocated **Storybook** preview and **Vitest**. The **Error not found** route uses it with the existing **`errorNotFound`** copy instead of an inline card and page-local **SCSS**.

### Bugfixes & Optimizations
- **Contributor docs**: **README**, **AGENTS.md**, **fantasia-testing**, **storybook-stories**, and **testing-terminal-isolation** describe **Storybook** **VRT** **toHaveScreenshot** **maxDiffPixels** as a whole-image differing-pixel budget, why **GitHub Actions** **windows-latest** can diverge slightly from local **Windows**, and when to refresh baselines versus adjust the cap.
- **Storybook** visual regression: raised **Playwright** **toHaveScreenshot** **maxDiffPixels** for static **Storybook** captures so **GitHub Actions** **windows-latest** passes **yarn test:storybook:visual** against committed baselines (fonts and subpixel differ slightly from typical local **Windows** runs).
- **GitHub Actions** **OSV-Scanner** workflow (**.github/workflows/osv-scanner.yml**) now lists **main** next to **master** on **push**, **pull_request**, and **merge_group** so the same scans run when the default branch uses either name.
- **Storybook** visual regression on **Windows**: refreshed committed **Chromium** snapshots under **.storybook-workspace/visual-tests/** for **ErrorCard** (error page and app settings search empty), **FantasiaMascotImage** reading, **MainLayout** with **Index** page, **ErrorNotFound** default, and **IndexPage** in layout — aligns baselines with current previews after native **img** mascot and layout test hooks.
- **Electron** security: the main **BrowserWindow** now runs the **Chromium** renderer with the **OS sandbox** on (**`sandbox: true`**, **`nodeIntegration: false`**). Harness values exposed as **`window.faContentBridgeAPIs.extraEnvVariables`** are built in the **main** process and delivered over sync **IPC**; opening external **http** or **https** links in the default browser is handled in **main** (**`shell.openExternal`**) via async **IPC** so preload stays within sandbox-safe **API** limits. **Playwright** launch paths use **`faPlaywrightElectronLaunchConstants`** in **`helpers/playwrightHelpers_universal/`** instead of importing preload-only modules from the Node test runner.
- **Electron** window-chrome sync IPC: **main-process** handlers for **minimize**, **maximize**, **restore toggle**, and **close** now set **`event.returnValue`** on every path. Without it, **`ipcRenderer.sendSync`** never completes and the **renderer** stays blocked, which froze the whole UI after the **@electron/remote** migration.
- Removed the **@electron/remote** dependency: frameless **window** chrome (**minimize**, **maximize**, **restore**, **close**, **maximized** state) and the packaged **app version** string shown in **About** now use synchronous **main-process IPC** from preload (**registerFaWindowControlIpc**, **registerFaAppDetailsIpc** in **mainScripts/ipcManagement/**), resolving the owning **BrowserWindow** from the IPC sender so behavior stays correct when native menus steal OS focus.
- **Window** chrome and **maximize** / **restore** interactions may feel slightly smoother in daily use after this change: the **renderer** no longer sits behind **@electron/remote** for those actions, and correct **`event.returnValue`** handling keeps **sendSync** from stalling the UI thread. Exact perception varies by machine and workload.
- Bumped **axios** to **1.15.0** to address **GHSA-3p68-rc4w-qgx5** (**CVE-2025-62718**): **NO_PROXY** matching now normalizes hostnames (for example **localhost** with a trailing dot) and bracketed IPv6 literals so loopback targets listed in **NO_PROXY** are not sent through an HTTP proxy.
- **App settings** persisted **boolean** keys in **electron-store** use corrected internal identifiers (**aggressive** relationship filter, **collapse** tree option, **after select** quick-search close).
- **Contributor workflow**: **README**, **AGENTS**, and Cursor rules and skills now describe when maintainers may skip **`yarn testbatch:verify`** immediately before a commit that changes only per-locale in-app changelog Markdown under **`i18n/*/documents/changeLog.md`**, provided the full gate already passed on the latest substantive edits and the working tree has no other pending changes; when in doubt, run the gate anyway.
- **Contributor docs**: root **CONTRIBUTING.md** covers fork **PR** labels, local **Storybook** **VRT** snapshot updates when **CI** cannot push baselines to forks, read-only **`GITHUB_TOKEN`** limits on public fork **`pull_request`** workflows, and maintainer **GitHub Actions** settings for fork approvals; **README** adds a **Contributing** section; the **pull request** template links **CONTRIBUTING.md**.
- **Repository** root **.gitignore** now ignores **.playwright-mcp** (workspace-local folders from the **Playwright MCP** server used with **Cursor** and similar clients; separate from **Playwright** test output under **test-results**).
- **Playwright** serial suites and **WebM** attachments: **`closeFaElectronAppWithRecordedVideoAttachments`** accepts an optional second **`TestInfo`** so screen recordings attach where the **HTML** report lists them for multi-test **`test.describe.serial`** groups; component and **E2E** specs use one **Electron** launch per serial group with **`resetFaPlaywrightIsolatedUserData`** in **`test.beforeAll`** only (**README** and **playwright-tests** describe the pattern). Vitest covers the updated **`playwrightElectronRecordVideo`** helper.
- **Playwright** component coverage for **ErrorCard** exercises the live harness (title, details, **reading** mascot, card max-width via **data-test-error-card-width**). Contributor guidance (**AGENTS**, **playwright-tests**, **fantasia-testing**) prefers **data-test-layout-width**, **data-test-layout-height**, and domain caps for sizing contracts when narrow windows would shrink **boundingBox** results; use **boundingBox** for rendered pixels only when the harness guarantees enough space.
- **Storybook** **Components/foundation**: design-time catalogues under **`src/components/foundation/`** — **`FoundationColorPalette`** (**QUASAR COLORS - GENERAL** from **`quasar.variables.scss`** plus default **Quasar** material **`bg-*`** stems) and **`FoundationTextList`** (**Quasar** typography: type scale, font weights, text helpers per **quasar.dev/style/typography**). Canvas-only (**Docs** disabled), **`skip-visual`** so **VRT** skips long pages; story globals pin **Reset viewport** (full-width iframe). **`FoundationColorPalette`** root **SCSS** stays transparent so **Backgrounds** toolbar (**paper** / **dark app**) paints the canvas; swatch metadata strips use the dark catalogue background from **quasar.variables.scss**.
- **Storybook** viewport toolbar adds **Reset viewport** (100% iframe) ahead of the phone-through-**4K** presets in **`viewportBreakpoints.ts`**; **`preview`** **`initialGlobals`** now start on that responsive preset instead of a fixed **1920px** desktop frame.
- **Storybook** visual regression on **Windows**: refreshed the committed **Chromium** screenshot for **Components/dialogs/DialogAppSettings** default so it matches the current **Reset viewport** default and **Storybook** frame.
- **Storybook** **App settings**: **`DialogAppSettings`** accepts optional **`directSettingsSnapshot`** (defaults mirror **`FA_USER_SETTINGS_DEFAULTS`**) so **Docs** and **Canvas** show the full category and toggle grid without **Electron**; a colocated decorator merges **en-US** **`appOptions.*.title`** strings with staging **TODO** dash prefixes stripped. The **Vite** alias mock for **`app/i18n/externalFileLoader`** implements **`global.mergeLocaleMessage`** (deep merge into scenario messages) and **`global.te`**, matching what **`buildAppSettingsRenderTree`** and the decorator expect from production **vue-i18n**. The **Storybook** **`faContentBridgeAPIs`** stub exposes **`faUserSettings`** with the same default map so **Pinia** hydration still works when a story omits the snapshot.
- **Storybook** component **`meta.title`** paths nest under **`Components`** by repo bucket (**dialogs**, **elements**, **globals**, **other**) plus the feature folder name so the sidebar matches **`src/components/`**. The **`yarn test:components:list`** picker shows **`dialogs/DialogFoo`**-style labels, and **`yarn test:components:single --component=`** takes that same relative path under **`src/components/`**. Refreshed committed **Storybook** visual baselines and **`EXCLUDED_STORY_IDS`** entries for the new story **`id`** shape; raised **`toHaveScreenshot`** **`maxDiffPixels`** to **220** in the visual suite so compare runs tolerate a little more font or subpixel variance on dense markdown dialogs without masking large UI drift.
- **SCSS** theme catalogue (**`quasar.variables.scss`**): Quasar palette, globals, **`q-*` adjustment tokens**, then per-feature sections; **`htmlAdjustments`**, **`scrollbar`**, **`quasarComponentsAdjustments`**, and matching **Vue** **`<style lang="scss">`** blocks use variables instead of scattered literals. Theme **`$`** identifiers use hyphen-separated segments only (see **`project-scss.mdc`**). Renamed the **qMarkdown** code background token to **`backgroundColor`** (was mistyped).
- **FantasiaMascotImage** renders mascot art with a plain **HTML** **img** (**object-fit: contain**, **display: block**) instead of **Quasar** **q-img**, avoiding **q-img** wrapper and loading layout jumps that disturbed tests while keeping **width**, **height**, **alt**, **`max-width: 100%`**, and **data-test** hooks on the image.
- In-app **changelog** and contributor docs: rephrased one **Playwright** bullet and expanded **vue-i18n** guidance so prose is not parsed as invalid placeholder syntax (curly-brace-shaped API snippets and similar).
- **App settings** search: **vue-i18n** placeholder (**en-US** / **fr**). With a non-empty trimmed query, the vertical **category** tabs stay visible but dim behind a short **Animate.css** fade and cannot be clicked; the right column stacks **matching** settings in one scroll (case-insensitive substring on **title**, **tags**, and tooltip **description**; non-matching rows and empty sections are omitted; filtering in **`scripts/dialogAppSettingsSearch.ts`** with Vitest coverage), with major section lines inset like the separators under each subcategory block. When nothing matches, a centered card uses the same cream **Error Not Found**-style border and fill as **`ErrorNotFound`**, with **FantasiaMascotImage** in **reading** mode and a dedicated **searchNoResultsMessage** string. While search is active, the no-results card and the matching-results column stay in the document with visibility toggled (**v-show**) so the **reading** mascot can load before the empty state is shown. The search field keeps a fixed width when **clearable** shows its icon, stays usable above the dim layer, and clearing no longer throws when the control sets the model to empty. Subtle fades soften the top and bottom of the stacked results and along the search strip while search is active; scroll regions use stable scrollbar gutter so showing or hiding a vertical scrollbar does not nudge the layout sideways. **SCSS** **BEM** names for the search wrapper, sticky titles, and footer actions match the template so rules apply.
- **`rgbToHex`** and **`hexToRgb`** in **`src/scripts/_utilities/colorFormatConvertors.ts`**: **`rgbToHex`** now maps only the first three numeric channels so **rgba()**-style strings (for example from **`getComputedStyle`**) do not append an alpha nibble to the hex result, requires at least three matches before converting, and parses channel integers with an explicit radix; **`hexToRgb`** trims the input and strips a leading **`#`** before parsing. Colocated **Vitest** coverage exercises the updated paths.
- **`toggleDevTools`** (**`src/scripts/appGlobalManagementUI/toggleDevTools.ts`**) optional-chains through the **`toggleDevTools`** method on **`window.faContentBridgeAPIs.faDevToolsControl`** so a missing implementation does not throw when the bridge is partially stubbed (for example in tests or **Storybook**).
- Expanded **Vitest** suites across **Electron** (**contentBridgeAPIs**, **preload**, **mainScripts** including **userSettings** cleanup for nullish or partial persisted snapshots), **renderer** (**boot**, **stores**, **dialogs**, **app settings** scripts, **globals**, **pages**), repo-root **i18n** tests, and **helpers/playwrightHelpers** so layered coverage thresholds stay aligned with these fixes.
- **SocialContactButtons** **webpage** styling: **quasar.variables.scss** sets the webpage chip background to **$dark** instead of **$qCard-background** so it reads clearly on the dark toolbar.
- **App settings**: subcategory-row and stacked-search top-level separators use **`showNonLastSeparator`** and **`showNonLastTopCategorySeparator`** from **`scripts/dialogAppSettingsSearch.ts`** with Vitest coverage; layout behavior matches the previous inline logic.
- Moved the entire **vue-i18n** locale tree from **`src/i18n/`** to repo-root **`i18n/`** (alongside **`src/`**); imports use **`app/i18n`** and **`app/i18n/...`**. **Vitest** **`unit-i18n`** (**`yarn test:coverage:i18n`**) enforces **100%** coverage on scoped **`i18n/`** entry **`.ts`** files; **README**, **AGENTS**, Cursor rules/skills, **Quasar** (**`@intlify/unplugin-vue-i18n`** include path), and **Storybook** aliases were updated.
- **vue-i18n** locale message files use an **`L_`** filename prefix (for example **`dialogs/L_appSettings.ts`**) so **`T_`** stays reserved for **TypeScript** type aliases; **Storybook** mocks, **Electron** spell-checker imports, tests, and contributor docs use the new paths.
- Local verification defaults are quieter: **Vitest** coverage uses the **agent** terminal reporter; **Playwright** component and **E2E** use the **list** reporter; **Storybook** static build and smoke use **quiet** CLI flags and calmer **Vite** production-preview logging; **Storybook** visual **Playwright** skips per-story **console** progress unless **FA_STORYBOOK_VISUAL_VERBOSE** is set; **`yarn testbatch:ensure:nochange`** and **`yarn testbatch:ensure:change`** invoke **`yarn quasar:build:electron:summarized`** so the full **Quasar** / **electron-builder** log is captured under **test-results** when a build fails. **README**, **AGENTS**, and Cursor rules and skills describe the scripts and opt-in verbose flags.
- **Playwright** runs (**`TEST_ENV`** **`components`** / **`e2e`**) use **`%APPDATA%/<package name>/playwright-user-data`** (e.g. **`fantasia-archive\playwright-user-data`**, not **`fantasia-archive-dev`**) so **user settings** (**`electron-store`**) stay separate from **`quasar dev`** and packaged profiles. Each component and **E2E** spec calls **`resetFaPlaywrightIsolatedUserData()`** in **`test.beforeEach`** so that folder is removed before every **`electron.launch`** and tests start from default persisted settings. The isolated profile folder name is shared through **`src-electron/mainScripts/appIdentity/playwrightIsolatedUserDataDirName.ts`** (no **`electron`** import) so Node-side reset and Vitest stay safe during test collection.
- Fixed packaged **Electron** **preload** path in **`mainScripts/windowManagement/mainWindowCreation`**: the main-process bundle resolves **`import.meta.url`** to the shared main chunk directory (not the **`windowManagement/`** source folder). Resolving **preload** and **icon** from that directory again loads **`electron-preload`** correctly so **`window.faContentBridgeAPIs`** (including **`extraEnvVariables`** for **`TEST_ENV`** / **`COMPONENT_NAME`**) is available; component **Playwright** runs route to **`/componentTesting/...`** instead of falling back to the default **Index** page.
- Split **`src-electron/mainScripts/`** into feature folders — **`appIdentity`**, **`chromiumFixes`**, **`ipcManagement`** (**`register*Ipc`** handlers), **`nativeShell`**, **`userSettings`**, and **`windowManagement`** — so **IPC** registration, **user settings**, window creation, and **Chromium**-adjacent fixes have stable import paths; **Vitest** and **Playwright** imports, **README**, **AGENTS**, and Cursor rules/skills follow the new layout.
- Follow-up contributor docs: **README** **Architecture** notes bundled **`electron-preload`** resolution under **`windowManagement/`**; **AGENTS** and **`electron-preload`** point **`faUserSettings`** **`electron-store`** to **`mainScripts/userSettings/`**; **`vitest-tests`** lists every **`mainScripts/<area>/_tests/`** bucket; **`fantasia-electron-main`** warns against an extra **`..`** when resolving preload from **`import.meta.url`** in packaged builds.
- Cross-cutting **Playwright** harness modules (**`playwrightElectronRecordVideo`**, **`playwrightUserDataReset`**, and their Vitest suites) live under **`helpers/playwrightHelpers/`**; specs import **`app/helpers/playwrightHelpers/...`**. **Vitest** **`unit-helpers`** runs **`helpers/**/*.vitest.test.ts`** via **`vitest/vitest.helpers.config.mts`**; shared Vitest workspace configs live under repo-root **`vitest/`** so the strict **`helpers/**/*.ts`** coverage gate applies only to real helper packages. Contributor docs (**README**, **AGENTS**, Cursor rules/skills) describe **`helpers/`** for future harness packages so the repository root stays for config, **`README`**, lockfiles, and **`scripts/`**.
- **Vitest** coverage tiers are documented and wired in **`vitest/`**: **100%** v8 thresholds on **src-electron** and **helpers** packages; **100%** on all four metrics for scoped repo-root **`i18n/`** entry **.ts** files in **unit-i18n** (**`specialCharactersFixer`**, registry **`index.ts`**, **`externalFileLoader.ts`**, and per-locale **`en-US`**, **`fr`**, **`de`** index modules — see **`vitest/vitest.i18n.config.mts`**); **100%** on all four metrics for **src** renderer **.ts** in **unit-src-renderer** (**src/boot**, **src/scripts**, **src/stores** only — **`specialCharactersFixer`** moved out with the **`i18n/`** tree); **100%** on all four metrics for merged colocated **.ts** files under **src/components**, **src/layouts**, and **src/pages** in **unit-components** (each area keeps its own include globs; **Storybook-only** **foundation/** catalogues stay excluded from that slice). **Vue** SFCs have no failing threshold; reports use **60%** watermarks so weak totals prompt review. **App settings** render-tree **.ts** builds optional **note** leaves with an explicit assignment (not a conditional object spread); tests cover empty snapshots and reuse when two settings share a category and subcategory; Vitest imports sorting helpers from **dialogAppSettingsTree** directly. **README**, **AGENTS**, **.github/workflows/verify.yml** comments, **eslint-typescript**, **vitest-tests**, **testing-terminal-isolation**, **git-conventional-commits**, **playwright-tests**, **vue-quasar**, and skills (**fantasia-testing**, **fantasia-dev-setup**, **fantasia-changelog-en-us**, **fantasia-electron-preload**, **fantasia-quasar-vue**, **git-conventional-commits**) describe the same policy.
- **Root** **vitest.config.mts** registers **five** projects with **extends** into **vitest/** so the workspace **root** stays the repository and globs such as **src-electron/** resolve correctly. **package.json** **test:coverage:electron**, **test:coverage:helpers**, **test:coverage:i18n**, and **test:coverage:src** load those files; **tsconfig.json** excludes **vitest/vitest.components.config.mts** from the app typecheck. Former root-level **vitest.*.config.mts** / **vitest.setup.ts** fragments moved into **vitest/**.
- **Vitest** **`unit-components`** now includes **`src/layouts/**/*.vitest.test.ts`** and **`src/pages/**/*.vitest.test.ts`** alongside **`src/components/**`**, with a **1:1** **`tests/<Name>.vitest.test.ts`** baseline for each **`MainLayout`**, **`ComponentTestingLayout`**, **`IndexPage`**, **`ErrorNotFound`**, and **`ComponentTesting`** SFC. **App settings** tree Vitest covers **`toSortedRecord`**, **`buildAppSettingsRenderTree`**, category ordering, **`showDocumentID`** mapping, and local-settings management. **Electron** main Vitest adds coverage for **`suppressChromiumDevtoolsAutofillStderrNoise`**, **`registerFaDevToolsIpc`**, **`PLAYWRIGHT_ISOLATED_USER_DATA_DIR_NAME`**, **`fixAppName`** when the product name is empty, expanded **mainWindowCreation** and **spellChecker** behavior, and **tweaks**; **`mainWindowCreation`** exposes **`assignAppWindowRefForTesting`** for unit tests only.
- **Renderer** and harness **Vitest** expansions: **boot** (**qmarkdown**, broader **externalLinkManagement** cases), **stores** index module, **scripts** utility tests (**colorFormatConvertors**, **mdListArrayConverter**), **AppControlMenus**; **Playwright** helper packages gained deeper **`playwrightElectronRecordVideo`** and **`playwrightUserDataReset`** unit tests. **eslint.config.mjs** includes **vitest/** in the lint scope; **yarn.lock** reflects dependency updates from this work cycle.
- **Playwright** Electron **WebM** attachments can show a **synthetic red cursor dot** that follows scripted pointer moves (optional **`FA_PLAYWRIGHT_CURSOR_MARKER=0`** to disable); **App settings** component spec retries **Quasar** live **tooltip** hover with **`expect().toPass`**, **`locator.hover`** with the **`force`** option enabled, and a slightly longer tab-panel settle delay to cut flakiness under **Electron**.
- **Playwright** **App settings** component spec adds serial tests for the header **search** field after the **en-US** tab walk: one match each via **title**, **tags**, and setting **description** substrings, then the no-results **ErrorCard** with the **reading** mascot, no-results body copy on **errorCard-details**, and a **650** pixel card max width via **data-test-error-card-width**.
- **App settings** help tooltip for the pronounced count divider now shows the vertical bar in the sentence: **vue-i18n** treats a bare **pipe** as message-format syntax, so the en-US string uses the documented backslash escape so the character renders literally.
- **Quasar** **ripple** is on by default again; **window** controls (**GlobalWindowButtons**) and the **AppControlSingleMenu** toolbar **`q-btn`** disable **ripple** so chrome and the menu strip stay visually steady.
- **DialogMarkdownDocument** increases the content **max-height** offset so tall dialogs still leave room for header and footer chrome when scrolling.
- Reorganized renderer **Vue** components under **`src/components/`** buckets — **`dialogs/`** (modal **`Dialog*`** SFCs), **`globals/`** (**`GlobalWindowButtons`**, **`AppControlMenus`**, **`AppControlSingleMenu`**), **`elements/`** (**`FantasiaMascotImage`**, **`SocialContactSingleButton`**), and **`other/`** (**`SocialContactButtons`**); mirrored locale modules under **`i18n/*/components/<bucket>/...`**, refreshed imports and **Storybook** mocks, fixed **`SocialContactSingleButton`** story imports after the move, updated **Storybook** VRT baseline for **App settings**, and revised contributor docs (**README**, **AGENTS**, Cursor rules/skills).
- Stopped optional **app settings** **note** strings from triggering **vue-i18n** missing-key warnings when no **note** key exists for a setting (resolve notes only after a key-existence check).
- Added a startup cleanup pass for the persisted **user settings** store so legacy or unknown keys that no longer exist in **`src-electron/mainScripts/userSettings/faUserSettingsDefaults.ts`** are removed and the sanitized config is auto-saved.
- Extracted all hardcoded **en-US** locale strings from **`i18n/en-US/index.ts`** into dedicated per-feature locale modules per component, dialog, page, and global feature; normalized all top-level and sub-level i18n keys to **camelCase** with a lowercase first letter across the app.
- Aligned **Storybook**'s **`externalFileLoader`** mock (**`.storybook-workspace/.storybook/mocks/externalFileLoader.ts`**) with that **camelCase** message tree (including **`globalFunctionality`**) and wired non-markdown copy through the same locale modules the app uses, so component and layout previews—and local **`yarn test:storybook:visual`**—render real strings instead of raw translation key paths.
- Centralized **main ↔ preload** **IPC** channel names in **`src-electron/electron-ipc-bridge.ts`** (including **DevTools** sync channels) so **Electron** handlers and **preload** helpers cannot drift to mismatched strings; removed the older **DevTools**-only channel module.
- Documented **`electron-ipc-bridge.ts`**, **`mainScripts/ipcManagement/register*Ipc.ts`**, and **IPC** registration during **`startApp()`** (**`mainScripts/appManagement.ts`**) in **README**, **AGENTS**, and Cursor **`electron-preload`** / **`fantasia-electron-preload`** / **`fantasia-electron-main`** / **`fantasia-sqlite-main`** guidance so contributors and agents follow one end-to-end pattern.
- Added **`src/vue-i18n-shim.d.ts`** so **vue-i18n** injected globals (**`$t`**, **`$d`**, **`$n`**, and related **`$*`** helpers) are declared on **`@vue/runtime-core`**, aligning **Vue** SFC template typing with **`tsc`** and clearing spurious missing-**`$t`** diagnostics in editors that resolve **`ComponentCustomProperties`** through the runtime-core path.
- Replaced the main-process **`sqlite3`** dependency with **`better-sqlite3`** (synchronous **SQLite** bindings for the planned database layer; still a native addon—rebuild after **Electron**/**Node** bumps as documented in **README**). **AGENTS**, **`fantasia-sqlite-main`**, and the **`electron-main`** stub comment now refer to **`better-sqlite3`**.
- Renamed Storybook-related **Yarn** scripts under **`test:storybook:*`**: **`yarn test:storybook:visual`** (with **`:headed`**, **`:update`**, **`:update:headed`**) replaces **`yarn visual:storybook:*`**; **`yarn test:storybook:smoke`** replaces **`yarn storybook:smoke`**. **`.storybook-workspace`** script keys match. Updated **README**, **AGENTS**, Cursor rules/skills, and plan notes.
- Split the full-project gate: **`yarn testbatch:ensure:nochange`** runs **`testbatch:verify`**, production **Electron** build, **Playwright** component + **E2E**, **`yarn test:storybook:smoke`**, and **`yarn test:storybook:visual`** (snapshot compare). **`yarn testbatch:ensure:change`** is the same through smoke, then **`yarn test:storybook:visual:update`** for intentional baseline refresh. Removed **`yarn testbatch:ensure`**. Updated **README**, **AGENTS**, **testing-terminal-isolation**, **fantasia-testing**, **fantasia-dev-setup**, **fantasia-release-build**, and **changelog-en-us** guidance.
- Removed the **GitHub Actions** Storybook visual-regression job from **`.github/workflows/verify.yml`** and renamed the workflow to **Verify** (push/PR runs **`yarn testbatch:verify`** only; **`.storybook-workspace`** install kept for ESLint). Storybook VRT stays **local** or via **`yarn testbatch:ensure:nochange`**. Updated **README**, **AGENTS**, **testing-terminal-isolation**, **fantasia-testing**, **fantasia-dev-setup**, and **fantasia-changelog-en-us** (omit-list wording).
- **GitHub Actions** pull request workflows **pr-full-suite-nochange** and **pr-full-suite-change** require exactly one repository label on the PR, **novisualchange** or **visualchange** (see **`.github/PULL_REQUEST_TEMPLATE.md`**). **novisualchange** runs **`yarn testbatch:ensure:nochange`** on **Windows** so **Storybook** screenshot names stay aligned with committed **chromium-win32** baselines; **visualchange** runs **`yarn testbatch:ensure:change`** and may push updated files under **`.storybook-workspace/visual-tests/`** when the PR branch lives in this repository (fork PRs still run tests but cannot receive that push). PRs that have both labels or neither fail the **PR full suite (ensure nochange)** guard until fixed; the **ensure change** guard exits success in that case so the same mistake does not produce two identical red checks. A single idempotent bot comment explains the rule (PRs stay open so a label can be added after open). **`.github/scripts/pr-label-guard.sh`** centralizes the label check for both workflows.
- **Contributor setup**: **`commitlint`** and **`husky`** validate **`git commit`** messages locally (**`commitlint.config.mjs`**, **`.husky/commit-msg`**, **`yarn install`** runs **`prepare`**). Allowed types match **AGENTS** (**feat**, **fix**, **test**, **chore**, **refactor**, **style**, **docs**); **`Merge …`** and **`Revert …`** lines are ignored. **README** and **AGENTS** describe hooks; **`.cursor/rules/git-conventional-commits.mdc`** documents the same policy.
- **GitHub Actions** **OSV-Scanner** workflow (**`.github/workflows/osv-scanner.yml`**) now calls **`google/osv-scanner-action`** reusable workflows at **v2.3.5** so **`scan-pr`** no longer pulls deprecated **`actions/upload-artifact`** v3 (GitHub was failing those runs on **Set up job**).
- Set **`actions/upload-artifact@v5`** on the manual **Build** workflow (**`.github/workflows/build.yml`**) for **Linux**, **macOS**, and **Windows** release artifacts, alongside **`actions/checkout@v5`** and **`actions/setup-node@v5`**, so artifact uploads stay on supported **GitHub Actions** runtimes.
- Bumped **Vue** to **`^3.5.32`** (root and **`.storybook-workspace`**), **`@types/node`** to **`^22.19.17`**, refreshed **root** and **`.storybook-workspace`** lockfiles with **`yarn upgrade`**, and set Storybook VRT **`toHaveScreenshot`** **`maxDiffPixels: 150`** so compare runs stay stable across rebuilds. Refreshed one **Win32** chromium snapshot affected by the toolchain bump.
- Renamed the markdown bullet-list helper to **`mdListArrayConverter`** (standard spelling) and normalized loop variable naming in **`specialCharacterFixer`**; **Tips, Tricks & Trivia** notifications and markdown document escaping are unchanged in behavior.
- Polished **en-US** copy in the **Advanced Search Guide**, **Tips, Tricks & Trivia**, and the **en-US** app name string for grammar, spelling, and clarity; fixed a missing article in the **README** Quasar CLI heading.
- Relocated every **`*.stories.ts`** file under the matching feature **`tests/`** tree (**`src/components/**/tests`**, **`src/layouts/**/tests`**, **`src/pages/**/tests`**) and pointed **`.storybook-workspace/.storybook/main.ts`** story globs at those paths so **Storybook** sits beside **Vitest** and **Playwright**; refreshed **Storybook** VRT snapshots (including **DialogAppSettings** default and **DialogMarkdownDocument** previews).
- Enforced expanded multi-line object literals project-wide via **ESLint** (**`object-curly-newline`**, **`object-property-newline`**) for **JavaScript**, **TypeScript**, **Vue**, and **JSON**, with **`yarn lint:eslint`** running **`eslint .`** from the repo root.
- Extracted small file-local interfaces and type aliases from **Vue** and **TypeScript** sources into colocated **`<filename>.types.ts`** modules where applicable (renderer boot, Pinia typings, **Electron** tests, **Storybook** mocks) and recorded the convention in **README**, **AGENTS**, and Cursor rules/skills.
- Extended **Stylelint** with **`stylelint-order`** so **CSS custom properties** and regular **declarations** stay in **alphabetical** order (autofix via **`yarn lint:stylelint:fix`**); widened **`yarn lint:stylelint`** to scan **`vue`**, **`css`**, **`scss`**, and **`sass`** under **`src/`** and under **`.storybook-workspace/.storybook/`** (build output under **`storybook-static/`** stays ignored).
- **Electron Playwright** runs attach **WebM** screen recordings per test; open **`test-results/playwright-report/index.html`** after **`yarn test:components`** or **`yarn test:e2e`** (or the **`:single`** scripts) to review videos under each test’s **Attachments** (files under **`test-results/playwright-report/data/`**). Each Playwright run **replaces** that HTML report; **`scripts/playwrightWithArtifactTrim.mjs`** removes **`test-results/playwright-artifacts`** after those yarn commands so scratch output is not kept between runs. Documented in **README**, **AGENTS**, and Cursor **fantasia-testing** / **playwright-tests** guidance; optional **`FA_PLAYWRIGHT_NO_VIDEO`** skips recording.
- Removed a stray **HTML** **textarea** from the **Index** page template.

---

## 2.4.9 - Contributor tooling, Pinia dialogs, and CI hardening

### Bugfixes & Optimizations
- Removed **`yarn visual:storybook:test:local`** and **`yarn visual:storybook:update:local`** (and matching **`.storybook-workspace`** **`storybook:visual:*:local`** scripts). For build + headed compare or snapshot refresh, use **`yarn test:storybook:visual:headed`** / **`yarn test:storybook:visual:update:headed`**; to run Playwright only when **`storybook-static/`** is already current, use **`yarn --cwd .storybook-workspace test:storybook:visual`** or **`test:storybook:visual:headed`** / **`test:storybook:visual:update`** / **`:update:headed`**. Updated **README**.
- Fixed **Pinia 3** dialog flow: **`S_DialogMarkdown`** and **`S_DialogComponent`** are now standard setup stores (call **`S_DialogMarkdown()`** / **`S_DialogComponent()`** at use sites). **`openDialogMarkdownDocument`**, **`DialogMarkdownDocument`**, and **`DialogAboutFantasiaArchive`** use guarded watchers when a store is not active (e.g. **Storybook**). **`ComponentTesting`** handles missing **`componentName`** routes safely and renders the harness only after a matching SFC is resolved.
- Simplified the **Index** preview page by removing the extra **Fantasia Mascot** block from the default **`IndexPage`** route (keeps the route as a lightweight shell for local links/testing).
- Renamed and grouped **Yarn** contributor scripts for clarity: quality gates **`yarn testbatch:verify`** / **`yarn testbatch:ensure`**; lint **`yarn lint:eslint`**, **`yarn lint:typescript`**, **`yarn lint:stylelint`**; Quasar Electron **`yarn quasar:build:electron`** / **`yarn quasar:dev:electron`**; Playwright **`yarn test:components`** and colon-suffixed **`test:components:*`**, **`test:e2e:*`**; Storybook dev **`yarn storybook:run`** and static **`yarn storybook:build`** (formerly **`build-storybook`**). Removed obsolete **`yarn test:full`** and duplicate **`yarn visual:storybook:ci`**; **GitHub Actions** call **`yarn visual:storybook:test`** directly. Updated **README**, **AGENTS**, Cursor rules/skills, **`package.json`**, **`testRunner_*.mjs`**, and workflows.
- Colocated Storybook **static output** (**`.storybook-workspace/storybook-static/`**), **Playwright** visual-regression config, **`visual-tests/`** (including committed snapshots), workspace **`storybook:visual:*`** scripts, and VRT devDependencies (**`http-server`**, **`@playwright/test`**) under **`.storybook-workspace/`**; root **`yarn visual:storybook:*`** delegates with **`yarn --cwd .storybook-workspace`**. Updated **`.gitignore`**, **`tsconfig.json`**, and **`lint:eslint`** paths.
- Set explicit least-privilege **`permissions`** (`contents: read`) on **`verify`** and manual **`build`** **GitHub Actions** workflows; added **Dependency Review**, **OSV Scanner** (reusable workflows + weekly schedule on **`master`**), and **Gitleaks** for PR/push hygiene.

---

## 2.4.8 - Pinia 3 and Vue Router 5

### Bugfixes & Optimizations
- Refreshed **Sass** to **1.99.x** under **`^1.78.0`** in **root** and **`.storybook-workspace`** lockfiles so the desktop app and **Storybook** use the same resolved Dart Sass line for styles.
- Upgraded **Pinia** to **v3** and **Vue Router** to **v5** for the desktop app and **`.storybook-workspace`** (explicit **`vue-router`** there for **`StoryRouterShell`**); did not add optional **`@pinia/colada`**. Refreshed **root** and **`.storybook-workspace`** lockfiles with **`yarn upgrade`** within existing semver ranges (**Storybook** remains pinned at **10.3.4** on that line).
- Upgraded **`uuid`** to **v13** for markdown and component dialog UUIDs (**`S_Dialog`**) and **`@quasar/cli`** to **v3** for the **`quasar`** CLI used with **`@quasar/app-vite`** v2.
- Upgraded **Electron** to **41.x** (from **33.x**); **`electron-builder`** still runs **`@electron/rebuild`** so **`sqlite3`** matches the bundled runtime. **`@electron/remote`** **2.1.x** and the known UnPackaged production-install peer warning for **`electron`** are unchanged.
- Standardized type-only imports across the TypeScript and Vue codebase (`import type`) to align with strict linting and keep runtime bundles free of type-only import noise.
- Added a new `yarn ensure` command for full-project validation (`verify` + production build + Playwright component + Playwright E2E) and aligned contributor guidance in **README**, **AGENTS**, and Cursor rules/skills to document the quick gate vs full gate workflow clearly.
- Reduced non-actionable Vue warning noise in component Vitest output by filtering known Quasar-resolution and test-only injection warnings in shared test setup, keeping signal focused on actionable failures.
- Fixed CI TypeScript resolution for Vue SFC imports by replacing the generated `.quasar` shim reference in `src/env.d.ts` with an in-repo `declare module '*.vue'` declaration, so `yarn verify` succeeds on clean GitHub runners without requiring local/generated `.quasar` shim files.

---

## 2.4.7 - Storybook 10 and Vite 8 alignment

### Bugfixes & Optimizations
- Upgraded **`.storybook-workspace`** to **Storybook 10** and **Vite 8** so component previews run on the same major bundler line as the desktop app (**`@quasar/app-vite`**), refreshed the nested **Yarn** lockfile for reproducible installs (including **CI** frozen installs), and aligned root **Storybook** / **`eslint-plugin-storybook`** versions with that release family.
- Refreshed **root** and **`.storybook-workspace`** lockfiles with **`yarn upgrade`** within existing semver ranges (notably **Quasar**, **Vue**, **vue-i18n**, **Playwright**, **Sass**, and related tooling); kept **`@types/node`** on the **22.x** typings line for the **Node 22** engine. Updated **README** and **AGENTS** so contributors see **Storybook 10** on **Vite 8** instead of the old split-Vite wording.
- Added **`debug-storybook.log*`** to **`.gitignore`** so local Storybook diagnostic logs are not picked up as untracked files.

---

## 2.4.6 - Window chrome, Storybook, Stylelint, and DevTools

### Bugfixes & Optimizations
- Applied **`getCurrentWindow()`** from **`@electron/remote`** to **window chrome** bridge helpers (**minimize**, **maximize**, **resize**, **close**, **maximized state**) so controls stay tied to the app window when OS focus moves to menus or other surfaces.
- Aligned **`.storybook-workspace`** with the main app on **Vue 3.5**, **vue-i18n 11**, **Quasar 2.16+**, **Pinia 2.3+**, and **@quasar/extras 1.17**; removed stray **`package-lock.json`** there so only **Yarn** lockfiles are used.
- Refreshed **root** and **`.storybook-workspace`** lockfiles with **`yarn upgrade`** within existing semver ranges; pinned root **Storybook** addons to **8.6.18** so they match **`@storybook/vue3-vite`** and the nested workspace (notably newer **Playwright**, **Sass**, and other compatible tooling).
- Migrated **`src/css/app.scss`** and **`globals/*.scss`** to Sass **`@use`** with explicit **`quasar.variables.scss`** imports in partials, replacing deprecated **`@import`** chains for app-owned global styles ahead of Dart Sass 3.
- Storybook’s Vite **`additionalData`** prepends **`quasar.variables.scss`** for project **`.vue` / `.scss`** (via **`file://`** URLs so Windows paths with spaces resolve). For **`quasar/src/css/index.sass`** it uses **`@import`** (not **`@use`**) ahead of Quasar’s own imports—the same effective ordering as **`@quasar/vite-plugin`’s `sassVariables`** transform—so **`!default`** theme entries resolve to your palette and framework CSS variables (**`--q-dark`**, **`--q-primary`**, and related tokens) match the desktop app; other **`node_modules`** Sass stays untouched.
- Extended the root **Stylelint** setup with **`@stylistic/stylelint-config`** so stylistic rules dropped from **Stylelint 17**’s core keep enforcing familiar CSS/SCSS formatting; updated **`.vscode`** recommendations and settings so the Stylelint extension can use the project config, **fix on save**, and format standalone **`.scss`** files consistently.
- Routed **DevTools** control (**toggle**, **open**, **close**, and **status**) through small **main-process IPC** handlers instead of **`@electron/remote`** **`getCurrentWindow()`**, so **Help → Toggle developer tools** stays reliable in packaged **Electron** builds; the **E2E** check now waits until DevTools finish attaching instead of asserting immediately after the menu closes.
- Updated **GitHub Actions** on **`verify`** and manual **build** workflows (**`actions/checkout`**, **`actions/setup-node`**, **`actions/upload-artifact`**) to current major versions.
- Noted in **`quasar.config.ts`** that **`@electron/remote`**’s **`electron`** peer is expected to show as unmet during UnPackaged **`yarn install --production`**; packaging still succeeds because the app runs on the bundled Electron runtime.
- Storybook preview registers Quasar’s **`Dark`** plugin, the **`@quasar/quasar-ui-qmarkdown`** app plugin (after Quasar, matching boot order), **Roboto (latin-ext)**, and **`preview-body.html`** / **`Dark.set(true)`** so **`body--dark`** stays aligned with the app; Sass **`additionalData`** skips **`app.scss`** to avoid double-loading variables. **Dialog** and **top-menu** component Docs use **`parameters.docs.story.inline: false`** again so Autodocs does not mount every story in one document (each **`q-dialog`** auto-opens on mount, which stacked modals when **`inline: true`**); each nested Docs iframe still loads the same preview CSS stack.

---

## 2.4.5 - Contributor workflow and CI

### Bugfixes & Optimizations
- Added **`yarn verify`** to run ESLint, TypeScript (`tsc`), Stylelint, and unit tests in one command (named **`verify`** because Yarn 1 reserves **`yarn check`** for dependency verification); documented it in the README and dev-setup guidance, aligned Cursor rules/skills, and run it in the manual GitHub Actions build workflow before the production Electron build.
- Fixed the combined unit-then-Playwright helper so it runs unit tests then Playwright over component and E2E paths (removed the broken `concurrently` invocation and dropped the unused **`concurrently`** dependency).
- Cleared the French locale changelog markdown so release notes are not maintained per locale until translations return.
- Upgraded **`@intlify/unplugin-vue-i18n`** to v11 for the Quasar/Vite locale pipeline, **`uuid`** to v11 (built-in TypeScript types; removed **`@types/uuid`**), and **`sqlite3`** to v6 for the Electron main-process dependency line.
- Added a **GitHub Actions** workflow that runs **`yarn verify`** on **push** and **pull request** to **`master`** / **`main`** (including installing the Storybook workspace so ESLint can resolve `.storybook-workspace/.storybook`).
- Refreshed **root** and **`.storybook-workspace`** lockfiles with **`yarn upgrade`** within existing semver ranges (notably newer **Playwright**, **Quasar**, **Vue**, **vue-i18n**, **Sass**, and related tooling where ranges allowed).
- Made **DevTools** bridge helpers use **`getCurrentWindow()`** instead of **`BrowserWindow.getFocusedWindow()`** so **toggle / status** stay tied to the app window when menus or DevTools change OS focus, stabilizing the DevTools **E2E** check.

---

## 2.4.4 - Dependencies and dev tooling

### Bugfixes & Optimizations
- Refreshed the dependency lockfile with `yarn upgrade` under the existing semver ranges so installs resolve to the latest compatible releases (including Quasar, Vue, `vue-i18n`, Sass, Playwright, and the Vite tooling pulled in by Quasar).
- Upgraded `jsdom` to v29 for the Vitest DOM environment, `eslint-plugin-n` to v17, and refreshed `@types/node` on the Node 22 typings line.
- Migrated lint and types tooling to **ESLint 9** flat config (**neostandard**, **eslint-plugin-vue** v10, **typescript-eslint**), **TypeScript** 6, and **Stylelint** 17 with updated shareable configs; small source tweaks match stricter defaults (import attributes, union spacing, dev window URL guard, QMarkdown CSS shim).
- Aligned **AGENTS**, Cursor rules, and skills so the standard pre-commit and pre-changelog workflow always runs **ESLint**, **TypeScript (`tsc`)**, **Stylelint**, and **unit tests** in that order; the **quality gate** may run as one chained terminal command for efficiency, with individual commands reserved for debugging failures. **Build** and **Playwright** checks stay one command per terminal.

---

## 2.4.3 - Additional dependency updates and improved testing workflow

### Bugfixes & Optimizations
- Upgraded `vitest` to v4 and adjusted Electron main-process unit-test mocks to constructor-compatible implementations so `BrowserWindow`, `Menu`, and `MenuItem` mocking remains stable under the newer runner.
- Refreshed additional low-risk dependencies (`sqlite3`, `lodash-es`, `stylelint`, `stylelint-config-standard-scss`, and `eslint-plugin-promise`) and revalidated the complete lint/test/build pipeline after the updates.
- Added persistent agent/testing guidance to run each validation command in an isolated terminal invocation to keep long test outputs readable and easier to audit.
- Aligned the `2.4.3` changelog block with the canonical `package.json` version and strengthened contributor guidance so changelog edits always re-read live `package.json` first, avoiding section/version drift.

---

## 2.4.2 - Core dependency refresh and Quasar/Vite workflow validation

### Bugfixes & Optimizations
- Upgraded `vue-i18n` to v11 and removed legacy TypeScript suppression in i18n bootstrapping now that current typings compile cleanly.
- Refreshed core app and tooling dependencies (including Quasar extras/qmarkdown, Axios, Pinia, Vue Router, Vitest, and selected ESLint plugins) to reduce maintenance risk while staying within the current project architecture.
- Applied a follow-up tooling refresh for the Quasar/Vite workflow (`@quasar/app-vite`, `@intlify/unplugin-vue-i18n`, `vite-plugin-checker`, `eslint-plugin-vue`, and `@types/node`) with full lint, typecheck, unit, component, E2E, and build validation.

---

## 2.4.1 - Changelog policy lock and component test stability

### Bugfixes & Optimizations
- Fixed Playwright component-test discovery for `DialogAboutFantasiaArchive` by avoiding full i18n loader imports that pull markdown documents into Node-side test loading.
- Updated the dialog social-button assertions to compare rendered labels, matching current UI output and keeping component tests stable.
- Locked project guidance to **never auto-bump versions**; changelog sections now strictly follow the existing `package.json` version unless a manual change is explicitly requested.

---

## 2.4.0 - Quasar CLI Vite v2 and Node 22 toolchain

### Bugfixes & Optimizations
- Upgraded the desktop app toolchain to `@quasar/app-vite` v2 (Vite 8), ESM `quasar.config.ts`, TypeScript 5.6, and ESLint feedback via `vite-plugin-checker` during dev/build.
- Standardized contributor and CI Node.js on **22.22.0 or newer** (required by the current Quasar CLI), refreshed GitHub Actions `setup-node`, and documented native-module rebuild expectations for `sqlite3` after Electron/Node bumps.
- Replaced the QMarkdown app extension with direct `@quasar/quasar-ui-qmarkdown` registration, Vite-native `*.md?raw` locale imports, and Vue template `isPreTag` handling so markdown dialogs stay stable without the extension’s Vite plugin.
- Migrated vue-i18n bundling to `@intlify/unplugin-vue-i18n`, bumped Electron and electron-builder for ESM main-process output, and aligned Sass with the Storybook subproject; Storybook remains on Vite 6 until its supported peer range includes Vite 8. *(Superseded in **2.4.7**: Storybook now runs on **Vite 8** with **`@quasar/app-vite`**.)*
- Bumped **`@electron/remote`** to 2.1.x so Electron 33 keeps a working preload (avoids removed Chromium feature probes that previously broke preload load and the bridge APIs).
- Fixed **Playwright component-testing** routing: `ComponentTesting` now globs `../components/**/*.vue` from `src/pages`, so production Electron runs mount the requested SFC instead of an empty harness.
- Raised **`@typescript-eslint`** to v8 for TypeScript 5.6, added **`yarn lint:types`** (full-project `tsc`), and expanded AGENTS/Cursor guidance for ESLint and typecheck gates (TSLint is not used).
- Reduced noisy **DevTools Autofill** CDP stderr lines in development by filtering those specific messages in the Electron main process (harmless protocol mismatches only).
- Hardened renderer code paths that call **`window.faContentBridgeAPIs`** when the preload bridge is missing, and aligned Vitest **`vi.stubEnv`** usage with Vitest 3 boolean rules for `DEV` / `PROD` / `SSR`.
- Tuned **Quasar/electron-builder** config for stricter typings and packaging: PWA `workboxMode` casing (`GenerateSW`), empty **`bex`** block matching current Quasar types, Linux **`desktop.entry`** for electron-builder 26, plus TypeScript project glue (`env.d.ts` Vue shim reference, vue-i18n augmentation handling) so `lint:types` stays reliable.
- Addressed **Dart Sass 2** deprecation noise in shared SCSS via `sass:color` in Quasar variables and scrollbar styles (replacing legacy color helpers).
- **Install reminder:** the **`electron`** package needs its **postinstall** download; skipping lifecycle scripts (for example `npm install --ignore-scripts`) can leave a broken binary until you reinstall or run `node node_modules/electron/install.js`.

---

## 2.3.2 - Storybook layouts, pages, and contributor tooling

### New features
- Added canvas-only Storybook previews for main layouts and representative app pages using a shared in-memory router (`StoryRouterShell`), a single `STORYBOOK_APP_ROUTES` table, and colocated `src/layouts/**` and `src/pages/**` stories.
- Wired a Storybook-only Vite transform that rewrites `import.meta.globEager` to eager `import.meta.glob` so the component-testing page loads under the Storybook workspace toolchain.
- Extended Storybook i18n mocks so the not-found page shows real copy instead of raw translation keys; simplified index and single-component story labels (dropped the redundant index-only preview).
- Documented layout and page Storybook expectations in `AGENTS.md` and Cursor rules/skills, including canvas-only previews and changelog ordering alongside component stories.

### Bugfixes & Optimizations
- Pointed root ESLint at intentional source and config paths instead of the whole repository; added `yarn lint:style` with Stylelint for `src` Vue and SCSS plus a root Stylelint config.
- Enabled TypeScript `skipLibCheck` and excluded build artifacts, Storybook static output, the Vitest components config, and colocated `*.stories.ts` from the root `tsconfig` project for cleaner checks.
- Removed an empty scoped style block from `SocialContactButtons` surfaced by style linting.
- Refreshed Vitest and Playwright coverage (external link manager, mascot image, window controls) to match current behavior and selectors.

---

## 2.3.1 - Electron packaging icon and favicon coverage

### Bugfixes & Optimizations
- Added explicit Electron Builder icon configuration for Windows, macOS, and Linux, including Linux desktop-entry metadata and Linux target setup.
- Linked all generated favicon sizes in `index.html` so renderer icon declarations cover the full `public/icons/` set.
- Updated changelog markdown wording to avoid build-time parsing failures in the qmarkdown/Vite pipeline during Electron production builds.

---

## 2.3.0 - Storybook workspace and desktop polish

### New features
- Moved Storybook into a nested `.storybook-workspace` package with its own toolchain, removed the root `.storybook` tree, and refreshed colocated stories (menus, dialogs, mascot, window controls, and social buttons) for stable addons, `public/` assets, and authoring workflows.
- Tuned global Quasar variable tokens and shared component SCSS adjustments for more consistent window chrome, controls, and markdown/dialog surfaces.
- Added Fantasia mascot image label strings for en-US and German locales to support accessible naming.
- Documented Storybook, Electron `file://`, and Playwright rebuild expectations for contributors in `AGENTS.md`, `README.md`, and Cursor skills/rules (including a dedicated Storybook stories rule).

### Bugfixes & Optimizations
- Fixed social contact button icons not loading in packaged Electron builds by resolving public image URLs with a relative base when the app base URL is "/" or empty, instead of root-relative URLs under file://.
- Updated Playwright component coverage for window controls to match current markup and accessibility labels.

---

## 2.2.1 - Type naming and consistency sweep

### New features
- Added explicit component test-governance guidance across AGENTS/rules/skills: each `src/components/**` Vue component keeps a colocated `*.vitest.test.ts` suite, with parity treated as coverage presence (not exhaustive branch/line percentages).
- Standardized fixture placement policy for future test authoring: Vitest and Playwright fixtures stay inline in their respective test files, while `_data/` remains production-only.
- Refactored shared menu and dialog/document type declarations into clearer singular and collection naming while preserving the `I_` / `T_` prefix conventions.
- Added reusable menu item and submenu type building blocks to keep menu configuration typing easier to maintain.
- Expanded renderer-side Vitest coverage under `src/` for scripts and store state transitions using deterministic mocks.
- Added a tracked renderer test-strategy inventory under `.cursor/plans/` to map `src/` TS/Vue targets across Vitest and Playwright responsibilities.
- Split Vitest into a Node/core config and a jsdom components config with shared setup so `yarn test:unit` covers boot, i18n, scripts, stores, and colocated smoke tests for every `src/components/*.vue`.
- Colocated `AppControlSingleMenu` and `SocialContactSingleButton` as top-level components (paths and imports updated).
- Routed initial navigation through a dedicated `appStartupRouting` helper consumed from `App.vue`.

### Bugfixes & Optimizations
- Fixed trigger callback typing mismatches in help menu data by wrapping typed handlers in zero-argument callbacks compatible with generic menu trigger signatures.
- Normalized typo-prone comments and wording across component tests, E2E tests, and selected source files for consistent naming and grammar.
- Updated README test command examples to match actual script names and usage patterns in `package.json`.
- Separated Playwright artifact output from the HTML report folder in `playwright.config.ts` to remove reporter output-directory clash warnings.
- Re-aligned testing guidance to keep `yarn test:unit` as the baseline unit-test workflow in project docs and Cursor guidance files.
- Clarified AGENTS/rules/skills guidance so Vitest explicitly treats both `src/` and `src-electron/` as first-class unit-testing surfaces while keeping Playwright as the integration/runtime layer.
- Clarified test-data rules: automated-test fixtures stay inline in each `*.vitest.test.ts` / `*.playwright.test.ts`, component-testing payloads use `COMPONENT_PROPS` where possible, and embedded component-mode menu data stays inline in `AppControlMenus` for dialog triggers.
- Resolved ESLint findings in boot external-link Vitest mocks (`Event` listener typing and padded blocks).

---

## 2.2.0 - Testing and agent tooling

### New features
- Expanded Vitest coverage for Electron preload bridge modules and main-process scripts (window controls, devtools, external links, app management, spell checker, and main window creation) using deterministic mocks.
- Playwright component tests for DialogAboutFantasiaArchive (replaces the previous TODO placeholder file).
- Cursor rule and skill for local plan documents in `.cursor/plans/` (gitignored), plus changelog guidance to read version-matching plan files for release context.

### Bugfixes & Optimizations
- Replaced remaining TypeScript `any` annotations in shared type declarations with `unknown` to keep menu trigger and Vue component typing stricter.
- Added explicit anti-`any` guidance to Vue, TypeScript scripts, Vitest, and Playwright rules/skills to keep future code strongly typed.

---

## 2.1.0 - Tooling and AI-assisted development

### New features
- Cursor project rules and skills for Vue/Quasar, BEM and scoped SCSS, Playwright and Vitest, Electron preload, global SCSS, conventional Git commits, and en-US changelog upkeep tied to the app version.
- AGENTS.md as the project entry point for AI-assisted development.

---

## 2.0.0 - The Big Rewrite

### New features
- A whole plethora since this is a full rewrite!

### Known issues
- No issues at the date of release.

### Bugfixes & Optimizations
- Too many optimizations to list since this is a rewrite.
- No bugs at the date of release.
